# Brief — w2-n1-hud-character

**Date:** 2026-09-27
**Tier:** 2
**Deliverable:** code

## Request

> Дальше A. Трек W (веб-клиент) — рекомендую. По ROADMAP после D1 и D2 идёт именно он, это основная линия разработки.
>
> (ROADMAP.md §3, трек W) | W2.N1 | HUD + персонаж / инвентарь / экипировка | 2 |
>
> Паритет веб/бот. Любая новая player-механика видна в обоих клиентах или явно помечена «только бот / только веб, потому что…». (ROADMAP §2.3)

> Очень важный момент. [...] То, что нельзя сделать в Web, потому что это завязано на очень жесткую привязку к Telegram-боту, к логике, то тогда ищем нейтральный, универсальный, правильный, логический, быстрый путь современного решения, где и Web, и Telegram будут сосуществовать. Если до этого нужно переписать логику Telegram, но сохранить игру, её механику, то делаем. [...] Поэтому мы не пытаемся накостылять HTML поверх Telegram. Мы пытаемся, по сути, с нуля переписывать, рефакторить то, что есть так, чтобы это работало и в Web, и в Telegram.
>
> (сообщение владельца посреди гриля, 2026-09-27; полный текст — ADR-190)

## Answers

1. **Экипировка** — Вынести в сервис: один сервис экипировки для бота и веба; смена «снять всё → надеть» атомарна; бот работает так же.
2. **Рендер** — Серверный HTML: вьюхи собираются на сервере из сервисов и подменяют #play-state; без JS работает; CSRF/дедуп/троттлинг — из готового.
3. **Навигация** — Нативно + остальное через мост: «Я», «Инвентарь», «Снаряжение» открывают нативные экраны; редкие кнопки на них шлют прежний callback через мост.
4. **Инвентарь** — Единый список + фильтр и поиск: всё, что несёт игрок, вкладками-категориями и поиском по имени; фильтр в браузере; склад базы — в N6.
5. **Требования** — Верно, стоп на плане. Уточнены по ADR-190 (сообщение владельца выше): каждый экран — сервис с моделью экрана, бот и веб — два рендерера поверх него.

## Asks

1. В `/play` всегда виден HUD: здоровье, выносливость, золото, уровень и опыт, клетка и биом, активная задача с живым таймером; обновляется после каждого действия и при опросе входящих.
2. «Я» — одна модель экрана персонажа в сервисе; карточка бота и нативный экран «Я» в вебе рисуются из неё и показывают одни и те же данные.
3. «Инвентарь» — одна модель инвентаря в сервисе; в вебе единый список всего, что несёт игрок, с вкладками по категориям и поиском по имени (без JS список работает, фильтр — улучшение); списки ресурсов в боте берут данные из той же модели.
4. «Снаряжение» — одна модель снаряжения в сервисе; в вебе видно оружие и броню, надетое отмечено, надеть и снять можно из веба; экраны снаряжения бота рисуются из той же модели.
5. Надевание и снятие — один сервис, его зовут и бот, и веб; смена атомарна (двух надетых в одном слоте не бывает); механика и гейт Арсенала для игрока не меняются, handler'ы бота становятся тонкими рендерерами.
6. Кнопки, которые нативный экран не покрывает (Страховка, «Куда ушло», Склад базы и т.п.), работают через мост, как сейчас.
7. Только `wildworld-ui.css`: HUD, слоты снаряжения и список инвентаря сначала в `ui-kit.html`; экраны без горизонтального скролла на 375 / 768 / 1440.
8. Экипировка недоступна (нет Арсенала) — экран показывает замок с объяснением и путём к нему, а не ошибку после тапа, в обоих клиентах.
9. Вердикты: паритет — оба клиента из одной модели (ADR-190); `/guide` — нет, механик не прибавилось; совет — нет, совет про игру на сайте уже есть.
10. Живой проход на preprod-testbot: HUD, «Я», инвентарь, снаряжение, надеть и снять в вебе — и увидеть результат в боте; и те же экраны в боте после рефакторинга.
