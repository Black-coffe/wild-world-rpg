# Черновик ответа Александру (08.09.2026)

Канал — на усмотрение владельца: личкой через бота (`sendMessage` по `telegram_id`) либо
репликой в общем чате. Вопрос задан публично, поэтому ответ в чате закрывает и молчаливых
читателей: молчание в общем чате читается как «не работает».

## Текст (черновик, ждёт вердикта редколлегии)

> Привет. Маяки работают — последний поставили вчера.
>
> Главное, что путает: маяк ставится **не на базе, а в той клетке, где ты стоишь** —
> дошёл до места, куда хочешь возвращаться, и там его оставил. Дальше он работает как
> точка возврата: прыгать к нему можно откуда угодно.
>
> Путь по кнопкам: **«🏠 База» → «📡 Маяки» → «Установить маяк здесь»**. Кнопка «📡 Маяки»
> есть на экране базы и тогда, когда ты от базы далеко, — раньше её там не было, и это
> была наша недоработка, а не твоя невнимательность. Сейчас поправлено.
>
> Что нужно, чтобы кнопка сработала: активная база, построенный «Центр телепортации»,
> сам маяк в сумке, и чтобы клетка была свободна — не под чужой базой и без другого маяка.
> Сколько маяков держать разом — зависит от твоего уровня и уровня Центра телепортации.
>
> Если дошёл до экрана «📡 Маяки» и кнопки установки там нет — значит маяка в сумке нет,
> на его месте будет «🔧 Скрафтить маяк». А если написано, что нужен «Центр телепортации», —
> под сообщением теперь есть кнопка сразу на стройку.

## Что в тексте намеренно НЕ сказано

- Точная формула лимита (`intdiv(уровень,10)` + уровень здания) — это балансное число,
  правило TIPS/GUIDE-coverage запрещает вшивать такие цифры в объяснения: они дрейфуют.
  Сказано качественно: «зависит от уровня и уровня Центра телепортации».
- Заряды и снятие маяка — отдельная тема, в ответе на «как установить» она лишняя.

## Правка после ревью (08.09.2026)

Первый черновик называл путь «🏠 База → постройка «Центр телепортации» → 📡 Маяки» и говорил,
что маяк ставится «с базы». И то и другое неверно: кнопка «📡 Маяки» стоит первым рядом прямо
на экране базы, а внутри постройки лишь дублируется; маяк же встаёт в клетке, где стоит игрок.
Ошибка приехала из отчёта разведки, который сам бриф позже опроверг в разделе «Поправка к разведке».
Ровно тот класс дефекта, который эта спека и чинит, — поэтому ловится он тут же.
