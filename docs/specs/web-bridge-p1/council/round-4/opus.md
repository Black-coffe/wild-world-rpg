<!-- seat: opus · model: claude-opus-5-5 · round: 4 · head: 60adf3aa · pack: be09bd1085fa · attempt: 1 · recorded: 2026-09-24T21:24:44Z -->
COUNCIL: web-bridge-p1 · round 4 · seat opus
MODEL: claude-opus-5-5
COURT: C:/laragon/www/mmorpg/.vulyk/court/web-bridge-p1/round-4
VERDICT: RED
ASSUMED CONFIG: Local (Laragon) code at the COURT commit; preprod-testbot and prod not reached. Single app node + single DB, cron→Worker, no queue
RAN: cat brief.md; targeted reads/greps of Play.php, WebActService, WebDelivery, BridgeClient, VirtualChat(+GuardMiddleware), UpdatePipeline, MapService, MapZoomService, LinkCodeService, AccountCabinet view, play/_play views, wildworld-play.js, wildworld-ui.css, ui-kit.html, WipeManifest, GuideCatalog, WebLinkCodeAction, 2026-12-11-* migrations; php -l on the 7 new migrations (all clean)
PATH: none walked live. Browser MCP is none, and the live Tier-3 pass on preprod is the Queen's manual smoke. Walked /play → act → pipeline → capture → store in code only
ASK 1: GREEN - /play through bot routes, no second rule set - run: grep processUpdate/SOURCE_WEB in UpdatePipeline.php; grep request_location|web_app|switch_inline|callback_game in app/ saw: web acts go through the same UpdatePipeline→Longman processUpdate(new Update(synthetic)); none of those Telegram-only button types exist, so text, callbacks and commands cover the whole surface
ASK 2: GREEN - virtual id, nothing reaches Telegram - run: read VirtualChat.php, WebDelivery::route, VirtualChatGuardMiddleware, grep "use Longman\TelegramBot\Request;" in app saw: range -(2^52+acct) is outside Telegram's 52 bits. Layer (a) routes virtual send* to the inbox and drops edit/other. Layer (b) Guzzle guard returns ok:false 403 without touching the network. No direct Longman Request users. CharacterProvisioningService calls ensureForAccount
ASK 3: RED - photos come with full caption - run: grep -n "unlink\|encodeFile\|uploads/tmp" app/Services/World/MapService.php; read WebDelivery::photoUrl/buildMsg saw: the map photo (MapService::showMapWithPlayer, reached from the "Карта" dock button and MapOverviewAction) is a temp file under public/uploads/tmp. photoUrl stores only its URL, then MapService line 176 `@unlink($tempFile)` deletes the file right after the send. On /play the map screen's <img> points at a deleted file (404 plus a console error), so the web player never sees the map. The caption survives. Every other photo lives under uploads/telegram and works
ASK 4: GREEN - background copies and bell - run: read WebDelivery::route/toInbox/characterForChat, Play::inbox, play.php saw: virtual chats get inbox only. For linked players a non-actor send goes to Telegram (route returns null) and a SOURCE_MIRROR copy goes to the inbox when the flag is on and the account has logged in. Background edits of synthetic ids are patched in place. /play shows the bell and unread counter, polled by JS
ASK 5: GREEN - bot unchanged for Telegram players - run: read UpdatePipeline::run and WebDelivery::route saw: the telegram source still rethrows and uses handle(). route() returns null for real chats except the synthetic-id edit path (ids >= 1e9, WebPlay::firstMessageId), which real private-chat ids do not reach. The mirror is a side-copy only and never blocks the send (try/catch, logged)
ASK 6: GREEN - web channel, CSRF, throttle, own character - run: grep VALID_SOURCES PlayerActionLogger; Filters.php csrf; Routes accountThrottle:play|inbox; read WebActService::owns/validate saw: 'web' is in VALID_SOURCES plus the 100003 migration. CSRF is global (only telegram/webhook exempt). 429 buckets are per account. The character comes from the session, owns() checks account_id, and a callback must sit on that character's own message_id keyboard
ASK 7: GREEN - web.play_enabled default off - run: sed 2026-12-11-100002_WebPlayEnabledSetting.php; read Play::gate saw: game_settings row is bool with value_bool=0 and default 'false', plus a rationale. The gate checks the flag first on every /play route, and the stub is shown otherwise
ASK 8: GREEN - /web, tip, guide tell the truth - run: read 100011 migration, WebLinkCodeAction::PLAY_HINT, GuideCatalog 'web', LinkCodeService::MSG_OTHER saw: all three say "log out of the other login and enter the code", which matches link() refusing another account. They stay true with the flag on or off. The tip ships as a migration with no numbers, Robi's tone and paired *
ASK 9: RED - "Играть" in the header and cabinet, clear stub - run: sed 55-70 app/Views/site/account_cabinet.php; grep link() in LinkCodeService saw: the header "Играть" always leads to /play, and the flag-off stub is clear. But the new cabinet no-character lock says "сначала привяжи персонажа из бота кодом /web", and the notice says "Привязать персонажа из бота". Anyone who sees the cabinet is logged in, and link() refuses a logged-in other account (MSG_OTHER). So the lock points to a path that fails, repeating the untrue binding promise this spec set out to remove. It also never offers /account/character, which is the web-only player's real path; the /play stub does offer it via can_register
ASK 10: GREEN - 375/768/1440, tokens, ui-kit, console - run: per-class grep of every .play-* used in views/JS against wildworld-ui.css and ui-kit.html; awk for radius/shadow/hex in the play block saw: every class is in both files except the play-input-form hook, which has no styles. No radius, shadow or raw colour. grid minmax(0,1fr), min-width:0 and overflow-wrap:anywhere guard against horizontal scroll. Not seen in a browser. The map-screen 404 in ASK 3 would add a console error
ASK 11: GREEN - WipeManifest - run: grep web_play|web_inbox|telegram_users WipeManifest.php saw: web_play_state and web_inbox are PLAYER_DATA by character, web_play_intents is TRANSIENT, and virtual ids live in telegram_users (IDENTITY_RESET)
ASK 12: N/A - preprod walk, linked and web-only - why: environment: this seat has no preprod session. Browser MCP is none and the live pass is the Queen's manual Tier-3 smoke
UNASKED: (1) Mirror copies of Telegram messages that are later edited in Telegram (timers, progress) stay stale in the web inbox, because only synthetic ids are patched. (2) The bell exists only on /play: a linked player browsing the cabinet or header sees no unread count. (3) The flag-off stub repeats "Игра на сайте скоро" in the title and the body. (4) Handled well: bootstrap /start dedups per character, and if it fails the text input and the "Меню" fallback still work. (5) Handled well: a failed act restores transport in finally, and JSON 400/429 answers in place
BREACH: none
