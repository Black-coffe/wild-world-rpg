<!-- seat: opus · model: claude-opus-5-5 · round: 2 · head: e8da6618 · pack: bd05f7270b7a · attempt: 1 · recorded: 2026-09-24T17:41:49Z -->
COUNCIL: web-bridge-p1 · round 2 · seat opus
MODEL: claude-opus-5-5
COURT: C:/laragon/www/mmorpg/.vulyk/court/web-bridge-p1/round-2
VERDICT: RED
ASSUMED CONFIG: Local (Laragon) code judged statically plus PHP harnesses; the COURT has no .env or DB. Preprod-testbot and prod were not reached.
RAN: reads/greps of app/Controllers/Play.php, app/Services/Web/*, UpdatePipeline, Request, migrations 2026-12-11-*, WipeManifest, Filters, views site/play*.php + _play/state.php + header + account_cabinet, wildworld-ui.css, ui-kit.html; php harness (scratchpad) driving WebDelivery::route with a fake WebScreenStore; php harness for VirtualChat::is/idForAccount + virtual-chat interception
PATH: Player door, web variant (/play) walked in code only: header/cabinet "Играть" -> gate (flag -> login -> own character) -> WebActService -> UpdatePipeline(web) -> capture -> screen store -> state view. No live walk (no DB/.env in COURT; Browser MCP none).
ASK 1: GREEN - /play drives bot routes - run: read WebActService::act/runBridged, SyntheticUpdateFactory saw: synthetic callback/message update from session identity -> UpdatePipeline::run(SOURCE_WEB) -> same dispatcher as the webhook. No second rules layer, and callbacks are limited to buttons on the character's own screens and inbox.
ASK 2: GREEN - virtual id, nothing to Telegram - run: php v.php saw: idForAccount(1)=-4503599627370497, is(virtual)=true, is(-1001234567890 group)=false; route('sendMessage'|'editMessageText', virtual chat) -> intercepted, never null. The VirtualChatGuardMiddleware is a network backstop that TelegramDeliveryProbe pushes. CharacterProvisioningService calls ensureForAccount.
ASK 3: RED - edit replaces screen - run: php h.php (capture, then editMessageText on message 1000000001 that sits in history) saw: {"sent":[],"edited_ids":[1000000001]}. WebScreenStore::applyCapture:96-120 then patches that message inside history and leaves the current screen as it was. History items render live inline keyboards (_play/state.php:151), and callbackAllowed accepts them. A tap on a past screen, answered by an edit (the usual callback pattern), changes a dimmed side/bottom entry while the main screen stays the same. An edit to an inbox message does become the screen (WebDelivery:245-248). "Исправление сообщения становится сменой экрана" holds only for the current screen. Dock, history, media-off caption: OK.
ASK 4: GREEN - background -> TG + inbox copy / inbox only, bell - run: read WebDelivery::route/toInbox/characterForChat, play.php saw: a virtual chat gets an inbox row only. A real chat that is not the current actor gets a mirror copy if the flag is on and the account has logged in (last_login_at not null); the Telegram send is unchanged. There is a bell with a counter (99+ cap), polled through /play/inbox.
ASK 5: GREEN - Telegram players unchanged - run: read Request.php:40,51 and UpdatePipeline::run saw: route() returns null outside capture for non-virtual chats, so the send goes to the transport as before. The only extra step is an inbox side-write behind the flag, wrapped in try. The webhook still throws on failure; only web swallows.
ASK 6: GREEN - web channel, CSRF, throttle, ownership - run: read migration 100003, Filters.php:89, Routes 263-266, AccountThrottleFilter, WebActService::owns saw: ENUM gains 'web' and the logger passes source=web. CSRF is global (only the webhook is exempt). accountThrottle:play (60/min) and :inbox (12/min). The character comes from the session only, with an account_id ownership check; no ids are taken from the request.
ASK 7: GREEN - flag web.play_enabled default off - run: read migration 100002, Play::gate saw: bool setting with value_bool 0 and default 'false'. The flag is checked first on every /play route; when off, even guests get the stub.
ASK 8: RED - /web, tip, guide tell the truth about web play - run: grep PLAY_HINT/GuideCatalog:1023/UpdateWebLinkTipForWebPlay saw: all three are static strings: "Когда игру на сайте откроют ... Пока там заглушка — играй в боте/здесь". None of them reads web.play_enabled. When the owner flips the flag, which is the planned next step (Ask 7) and needs no deploy, all three go false. A web-only player who opens /guide inside /play is told there is only a stub. The linking wording ("выйди из другого входа и введи код") is correct and the tip is Robi-toned with no numbers.
ASK 9: GREEN - "Играть" in header + cabinet, stub when off - run: read header.php:61,93, account_cabinet.php:64-71, play_stub.php saw: link present on the desktop nav and in the mobile drawer. The cabinet shows a lock line (flag off / no character) or the button. The stub gives a reason and a path (bot, cabinet, code, create).
ASK 10: GREEN - 375/768/1440, tokens, ui-kit - run: grep hex/rgba/radius/shadow in wildworld-ui.css:925-1020 saw: 0 hits. Everything uses var(--*). min-width:0 and overflow-wrap:anywhere are set throughout; pre has overflow-x:auto. ui-kit.html carries all 35 .play-* classes. Viewport and console were not rendered: static review only.
ASK 11: GREEN - WipeManifest - run: grep WipeManifest.php saw: web_play_state and web_inbox are PLAYER_DATA by character_id, web_play_intents is TRANSIENT. Virtual ids live in telegram_users (IDENTITY_RESET, already classified); there is no separate table for them.
ASK 12: N/A - preprod pass via /play, linked + web-only - why: environment: the branch is not on preprod-testbot and the COURT has no DB/.env; the live walk belongs to the Queen's Tier-3 smoke.
UNASKED: (1) The bell exists only on /play. There is no unread indicator in the site header or cabinet, so a linked player browsing the site misses mirrored messages. (2) Inbox writes are skipped while the flag is off, so web-only characters built before enabling have no backlog. (3) A web-only player who hits a Telegram-only flow (referral t.me deep link, /web code for the bot) gets a text addressed to a Telegram user. Handled nowhere, but not in scope. (4) The stored last_map_message_id and similar ids cross between the real chat and web synthetic ids (1e9+). The edit->send fallback (R5) covers it, which is sound.
BREACH: none. Note: this session's injected context included recent commit subjects naming a story id (web-bridge-p1-08). I did not request them, did not rely on them, and every verdict above is from COURT files and harness runs.
