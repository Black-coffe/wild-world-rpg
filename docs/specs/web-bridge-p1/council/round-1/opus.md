<!-- seat: opus · model: claude-opus-5-5 · round: 1 · head: 7c71c41b · pack: bcab1c9c706e · attempt: 1 · recorded: 2026-09-24T17:10:29Z -->
COUNCIL: web-bridge-p1 · round 1 · seat opus
MODEL: claude-opus-5-5
COURT: C:/laragon/www/mmorpg/.vulyk/court/web-bridge-p1/round-1
VERDICT: RED
ASSUMED CONFIG: Local Laragon (MySQL up; local `mmorpg` DB lacks the 2026-12-11 migrations, COURT has no .env) — code-level judgement; preprod-testbot and prod not reachable from COURT
RAN: cat brief.md / Profile; reads of Play.php, WebActService, WebDelivery, BridgeClient, VirtualChat, VirtualIdentityService, VirtualChatGuardMiddleware, UpdatePipeline, WebScreenStore, views site/play*.php + _play/*, header.php, account_cabinet.php, WebLinkCodeAction, GuideCatalog, Routes/Filters, AccountThrottleFilter, WipeManifest, 2026-12-11 migrations; grep for Longman Request bypass / Probe::install sites / api.telegram.org; php scratchpad/probe_virtual.php (VirtualChat + guard middleware on a Guzzle MockHandler); mysql read-only SELECTs on information_schema (unsigned / narrow telegram-id columns)
PATH: none walked — no browser for this seat and no preprod access from COURT; the /play flow was traced in code from the header link to the gate, act, capture and view
ASK 1: GREEN - /play through the bot routes - run: read UpdatePipeline/WebActService saw: the webhook (BotController:77) and /play share UpdatePipeline::run; web builds a synthetic update and hands it to Longman processUpdate; callbacks, text (force_reply reply_to) and /commands are all accepted; there is no second copy of the game rules
ASK 2: GREEN - virtual id, nothing reaches Telegram - run: php probe_virtual.php saw: acct1 -> -4503599627370497 is=true; group -1001234567890 is=false; a virtual sendMessage/sendPhoto(multipart) returns 403 "web-only chat is not reachable" and never hits the network; a real id passes through. Layer (a) WebDelivery::route is in the only Request wrapper (0 direct Longman Request imports). The guard is installed in the webhook, Worker, TelegramBridge and /play
ASK 3: GREEN - screen/edit/dock/history/caption - run: read WebDelivery::captureEdit, WebScreenStore::applyCapture, _play/state.php saw: a send pushes the old screen into history (historySize 10); an edit replaces the message in place; the reply keyboard becomes .play-dock (with a /menu fallback); the caption is always rendered in figcaption; photo_url is null for non-public files, and then the caption alone carries the screen
ASK 4: GREEN - Telegram + inbox copy / inbox only / bell - run: read WebDelivery::route saw: a virtual chat gets only an inbox row; a real chat that is not the actor gets a mirror row (if the account has logged in on the site) and the message still goes to Telegram; the bell with unread count 99+ is on /play; inbox polling is on
ASK 5: GREEN - bot unchanged for Telegram players - run: read route()/UpdatePipeline saw: for a non-virtual chat outside /play, route() returns null (the send goes as before); the mirror is a DB side-write wrapped in try/catch; the webhook rethrow semantics are kept (only web swallows errors)
ASK 6: GREEN - web channel, CSRF, throttle, own character only - run: read migration 100003, Filters, Routes, Play::gate, WebActService::owns saw: source ENUM +'web'; global csrf excepts only telegram/webhook; accountThrottle:play|inbox has a per-account bucket that returns 429; character_id comes only from the session, owns() rechecks it, and a callback is allowed only if it is on the character's own screen/history/inbox
ASK 7: GREEN - web.play_enabled flag, default off - run: read migration 100002 saw: game_settings bool value 0, with rationale/effect/above/below text; it is checked on the server in every /play route and in inbox writes
ASK 8: GREEN - /web, tip, guide tell the truth - run: read WebLinkCodeAction::codeMessage, migration 100010, GuideCatalog:1006-1025 saw: all three say "выйди из другого входа и введи код" (F1 holds); the tip is updated by migration, has no numbers and is in Robi's voice; play-in-browser is phrased conditionally ("когда откроют … пока заглушка")
ASK 9: RED - "Играть" easy to find + stub while flag off - run: grep header.php:29-31,61 + read Play::gate saw: header "Играть" -> /play is shown to every visitor (its comment promises the stub "вошёл или нет"), but gate() checks the login BEFORE the flag: an anonymous visitor with the flag off lands on /account/login, not the stub, and after logging in is not returned to /play (no return-to anywhere in Account*). The cabinet lock text is fine
ASK 10: N/A - 375/768/1440, tokens, ui-kit, console - why: environment: no browser for this seat (Browser MCP none, haiku owns that row); static scan clean: .play-* uses var(--…) tokens with minmax(0,1fr)/overflow-wrap:anywhere, no inline style in play views, all 36 .play-* classes are in ui-kit.html
ASK 11: GREEN - WipeManifest - run: grep WipeManifest.php saw: web_play_state and web_inbox are PLAYER_DATA by character; web_play_intents is TRANSIENT; virtual ids live in telegram_users (IDENTITY_RESET, already classified; no new table)
ASK 12: N/A - preprod walk, linked + web-only - why: environment: preprod-testbot is not reachable from COURT, and the local DB has not had the phase-1 migrations applied (last is 2026-12-10-100020)
UNASKED: (1) While the flag is off, route() drops sends to virtual chats without writing to the inbox. Web-only characters from P0 lose raid/craft notices forever; nothing is queued for when the flag goes on. (2) A button pressed on a HISTORY screen whose handler edits that message updates the history item in place and leaves the main screen unchanged. The player sees "nothing happened" (applyCapture:96-101), while an inbox edit IS promoted to the screen. (3) After the owner flips the flag, /web, the tip and the guide keep saying "пока там заглушка — играй здесь", so another migration will be needed. PLAY_HINT could read the flag. (4) The bell and unread count live only on /play and not in the site header. (5) In an inbox item the photo is dropped (only text/caption is shown). This is acceptable under media-off, but inconsistent with the screen. (6) The good part: the virtual range sits beyond 2^52, so it never collides with a negative group id. Check `VirtualChat::is` and never test the sign alone. The unsigned action_log.chat_id is fixed by migration 100005, and no narrower telegram-id column remains locally.
BREACH: none — read-only SELECTs on the local `mmorpg` MySQL (migrations, information_schema); no files outside COURT except this report and the scratchpad probe
