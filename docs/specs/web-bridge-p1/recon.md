# Recon — web-bridge-p1 (2 drone-scout, 2026-09-24)

Directory-wide grep failed for both scouts (env_subagent_glob_and_dir_grep_empty) — counts marked «не проверено» are from report.md / P0 recon, not fresh.

## A. Dispatch path (scout 1)
- `BotController.php:16-28` builds `new Telegram` per request (no singleton); `webhook()` :47-178: secret :49-56 → decode :58 → dedup `telegram_updates_seen` :68,207-235 → group filter :77 → LastSeen extract :87 → `ActionOrigin::stripUpdate/set` :97-110 (setCustomInput) → firehose `PlayerActionLogger::begin/setOrigin` :119-122 → `TelegramDeliveryProbe::install()` :126 → E6/E8 hooks by telegram id (ReturnDigest :132-138, LoginStreak :141-145, DailyTask :149-153) → `dispatchToTelegram()` → `handle()` :368-371 → finally stampByTelegramId / commit / ActionOrigin::reset :167-177.
- Reusable dispatch: `vendor/longman/telegram-bot/src/Telegram.php:595 processUpdate(Update)`. No app-side "array → dispatch" function; body :83-177 would be extracted.
- Transport: `App\Services\Telegram\Request extends Longman Request` (`app/Services/Telegram/Request.php:28-44`, overrides send/sendMessage for KeyboardNormalizer). Everything goes to `Request::$client->post()` (`vendor/.../Request.php:593-617,691-719`).
- R4 statics (`vendor/.../Request.php:149-191`): `$client` persists per process; `initialize()` :376-380 keeps an existing client; `setClient()` :387 no getter → restore = new Client (as Probe :89-92). Probe `install()` idempotent via static `$installed` :59,74-77, `reset()` does not restore client — **Probe and the bridge client would overwrite each other; order must be explicit.** Fake client must return `{"ok":true,"result":{message_id…}}` else "invalid response" :714-716. Under PHPUnit `send()` short-circuits before the client :698-702 — interception untestable via PHPUnit as is.
- MediaSender (`app/Services/Notifications/MediaSender.php`) uses Request:: for sendPhoto/edit*; photo arrives as multipart file stream (`encodeFile` fopen) — interceptor must parse multipart or capture the path earlier.
- Player resolution everywhere: `from.id` → `telegram_users.telegram_id` → `telegram_users.id` → `characters.telegram_user_id` (FK to telegram_users.id). `BaseAction::getUserAndCharacter` :35-50, `BaseShiftingCommand::lookupUserAndCharacter` :90-99, `CallbackqueryCommand::handleCharacterShortcut` :130-143, `GenericmessageCommand` 13+ manual lookups, `StartCommand` :27,59-63,85. Synthetic update needs `from.id` and `chat.id` (and `message.chat` inside callback). Web-only character (telegram_user_id NULL) → every resolver returns null. Cheapest: virtual `telegram_users` row (type/unsigned of `telegram_id` — не проверено); refactor to character_id = dozens of places.
- Edit-in-place: `GenericmessageCommand:311`, MediaSender editOrSend :110-118,146-147,225,246; total counts не проверено. Interceptor needs stable synthetic message_id and edit-vs-send distinction.
- Markup: inline_keyboard, reply keyboard (`StartCommand:272`), force_reply replies (`GenericmessageCommand:50`), `parse_mode Markdown`, answerCallbackQuery show_alert (`CallbackqueryCommand:70-74`).

## B. Outbox / site / firehose (scout 2)
- Worker send helpers: `BaseTaskHandler.php:90 safeSendMessage($chatId)` (dedup `NotificationDedupService::shouldSuppress((int)$chatId)` :95-97 → `Request::sendMessage` :106), `:134 safeSendPhoto` → MediaSender, `:230 degradeToText`. Take chat id, not character. `TelegramChatResolver.php:28 chatIdForCharacter`, `:58 telegramUserIdForCharacter` — null = skip after reward. Bypass senders: 9 direct in 8 TaskHandlers, 62 in 33 services (не проверено); broadcast/nudge/digest iterate `telegram_users`.
- Task creators writing `$user['id']` as telegram_user_id: verified `GatherAction.php:257`; list from P0 recon (PlantCropActionStart:107, StartRobotExploration:215, StartRobotGathering:241, GenericBuilding:204, GenericCraftActionStart:230, StartrobotexplorerCommand:294, 7 legacy armor/teleport starters, MarchAction:268, RepairCraftedItem:251, DeleteBase:425, RelocationTaskCreator:71). `ExploredCellsModel::revealAround(int $characterId, int $telegramUserId)` with 0 → FK violation. `CharacterTaskModel.php:32` validation `required|integer` after P0 — не проверено.
- Firehose: `PlayerActionLogger.php:41 VALID_SOURCES` has no `web` (unknown → 'other' :262); DB `source` is ENUM (STRICT) → migration needed; `origin` 16 chars :149-152,282. Killswitch `logging.player_actions.enabled`.
- Site: `/account` group `Routes.php:237-258`, `accountThrottle` filter (`Filters.php:46`), CSRF global except webhook (`:88`), `AccountSession::current()` → `{account_id, character_id, telegram_user_id}`; CSP off.
- P0 text bug: `WebLinkCodeAction.php:67-68`, migration `2026-12-10-100020_SeedWebLinkTip.php:31-33` (idempotent → needs UPDATE migration), `GuideCatalog.php:1019-1021` (section `web`).
- Flag pattern: `2026-12-10-100003_WebOpenRegistrationSetting.php:28-47` (category world, bool, rationale/effect/above/below), `game_settings`=KEEP.
