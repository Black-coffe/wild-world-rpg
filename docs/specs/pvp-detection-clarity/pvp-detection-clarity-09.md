---
story: pvp-detection-clarity-09
spec: pvp-detection-clarity
status: done
tier: 2
worker: worker-code
tracer: false
wave: 4
blocked_by: [pvp-detection-clarity-06, pvp-detection-clarity-08]
---

# Три хода защитника: укрыться, убежать, ударить первым

## Goal

После story у защитника, поднявшего тревогу, есть реальные ходы. «🛡 Укрыться» — новое действие:
остаться, немедленно отдать инициативу и получить надбавку к снижению урона; окно закрывается как
`held`. «🏃 Убежать» — существующий побег, который теперь ещё и закрывает окно как `fled` и сообщает
нападавшему, что цель ушла. «⚔️ Ударить первым» ведёт на существующий `attackPlayer_` и обслуживается
story `-08`. Молчание остаётся правом: ничего не нажав, защитник просто доживает окно.

## Requirements

> Сбежать от атаки? атаковать в ответ? просто бах - вас атаковали)

> и стою я на базе ... .

> укрыться, убежать, первым атаковать и так далее

## Files
- app/Controllers/Telegram/Commands/Actions/PVP/StandoffHoldAction.php
- app/Controllers/Telegram/Commands/Actions/PVP/RunAwayAction.php
- app/Config/CallbackRoutes.php
- tests/database/StandoffDefenderMovesTest.php

## Non-goals
- Не менять саму механику побега: прыжок 10–50 клеток, 1000 золота, −50 % HP, −90 % выносливости остаются как есть; добавляется только закрытие окна.
- Не делать «укрыться» неуязвимостью на время: это кнопка «остаться и отдать инициативу», а не безопасная комната.
- Не трогать `AttackPlayerAction` — story `-08` держала этот файл предыдущей волной и уже закоммичена. Третий ход защитника («⚔️ Ударить первым») поэтому живёт не здесь: кнопку рисует `-06`, путь до боя закрывает `-08`. Эта story отвечает за два хода из трёх, и её название перечисляет все три только потому, что игрок видит их одним экраном.
- Не вводить новых полей в бой: надбавка едет существующим `damage_reduction` и применяется в `-08`.
- Не заводить свою проверку «окно ещё живо»: только `PvpStandoffService` из `-06`.
- Не запускать полный набор и не делать `DROP`/`migrate` на общей локальной тест-БД; не делать `git stash`/`git checkout`.

## Map slice
`app/Controllers/Telegram/Commands/Actions/PVP/RunAwayAction.php:37-200` (побег, callback `runAway`);
ADR-186 §3 (три хода и почему «укрыться» именно такой), «Инварианты» 4 и 8;
`## Contracts` плана — `close()`, callback_data `standoffHold_<id>`, аудит-коды.

## Acceptance criteria
- [ ] «🛡 Укрыться» закрывает окно как `held` через `close()`; атакующий немедленно разморожен и получает сообщение, что цель приготовилась к бою. Надбавка к снижению урона выдаётся не сразу в цифрах, а отражена в статусе окна — считает её `-08`.
- [ ] Побег при живом окне закрывает его как `fled`; нападавший получает сообщение «цель ушла», а его следующий тап всё равно отбивается проверкой смежности, потому что цели рядом больше нет.
- [ ] Побег вне окна работает ровно как сегодня — регресс закрыт тестом.
- [ ] Второй ход защитника (двойной тап, «убежал» и «укрылся» подряд) даёт ровно один применённый переход; второму игрок получает внятное «ты уже отреагировал», а не ошибку и не второй побег с повторным списанием золота.
- [ ] Ход по чужому или истёкшему окну отклоняется объяснением, а не исключением.
- [ ] Сообщения — HTML, экранированные имена, самодостаточный текст без фото, кнопки по 2–3 в ряд; текст тревоги объясняет механику целиком (что происходит, сколько осталось, что делает каждая кнопка) — это и есть just-in-time-обучение по ONBOARDING-coverage, отдельного шага в обучающем потоке спека не заводит.
- [ ] Каждый ход пишет строку аудита кодом из `## Contracts`.
- [ ] Тест строит схему из настоящих классов миграций и покрывает: `held`, `fled`, двойной тап, чужое окно, побег без окна.
- [ ] В `## Implementation notes` сказано: кнопки и их поток PHPUnit не проверяет — только Tier-3 двумя аккаунтами.

## Verification
`vendor/bin/phpunit --no-coverage --no-progress tests/database/StandoffDefenderMovesTest.php`
`vendor/bin/phpstan analyse --memory-limit=512M --no-progress`

## Implementation notes

- `StandoffHoldAction.php` (новый) — «🛡 Укрыться»: находит строку по `standoffHold_<id>`,
  проверяет `defender_id === текущий персонаж`, зовёт `PvpStandoffService::close($id, 'held')`.
  На успехе — подтверждение защитнику + попытка немедленно уведомить атакующего (best-effort,
  `try/catch TelegramException`, как `StandoffLeaveAction::notifyDefenderAttackerLeft()`). На
  отказе (`close()===false`) — «Ты уже отреагировал», не exception. Чужой defender_id — «Это не
  твоя тревога», тоже без exception.
- `RunAwayAction.php` — после обновления `cell_number` (сама механика побега не тронута —
  Non-goal) читает `PvpStandoffService::activeAgainst($character['id'])`; если окно живо —
  `close($id, 'fled')` + best-effort пинг атакующему («цель ушла»). Вне окна — `activeAgainst()`
  возвращает `null`, поведение байт-в-байт как раньше.
- `CallbackRoutes.php` — добавлена ровно одна строка `'standoffHold' => StandoffHoldAction::class`
  рядом с `standoffCheck`/`standoffLeave` (владеет `-08`), обновлён комментарий (снята пометка
  «регистрирует story `-09`» как выполненная).
- Обе новые уведомительные надписи атакующему написаны в present tense («остаётся», «убегает»),
  а не past tense («осталась»/«убежала») — сознательно: past tense в русском требует
  грамматического рода, а имя персонажа с другой стороны экрана рода не даёт (тот же приём,
  что уже в `buildAlertText()`/`waitScreen()` у `StandoffNotifier`, «стоит», не «пришёл/пришла»).
- **Кнопки и их поток PHPUnit не проверяет** — только состояние БД, аудит-код и текст,
  возвращённый `handle()` тому, кто нажал. Доставка сообщения ДРУГОМУ участнику
  (`notifyAttackerDefenderHeld`/`notifyAttackerTargetFled`) в тестах не перехватывается (нет
  общего спая исходящих `Request::sendMessage`, как у `StandoffLeaveAction`/`StandoffCheckAction`
  из `-08`) — реальная доставка и рендер обоим участникам проверяются только Tier-3
  (MCP Chrome + Telegram Web, два аккаунта).
- Тест собирал схему из настоящих классов миграций на изолированной локальной БД
  (`wildworld_ci_pvp09`, создана и удалена этой сессией) — общий `wildworld_tests` не трогался.
- Дистанция побега в тесте сделана детерминированной без правки прод-кода: у персонажа с
  `level=1, health=1, tired=1` формула `RunAwayAction` даёт `distancePossible < 10`, что
  клампится ровно к `10` (`distanceMin`), поэтому `rand(10, 10) === 10` — заранее готовятся
  клетки на дистанции 10 во всех 8 направлениях.

## Findings

Не найдено ничего, что потребовало бы отклонения от контракта плана или `## Files`. Единственная
неочевидная вещь — гендерная нейтральность текста уведомлений атакующему (см. заметку выше);
решена present tense без правки чужих файлов (`StandoffNotifier` вне `## Files` этой story).
