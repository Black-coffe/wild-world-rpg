---
story: pvp-detection-clarity-11
spec: pvp-detection-clarity
status: todo
tier: 1
worker: worker-code
tracer: false
wave: 4
blocked_by: [pvp-detection-clarity-08, pvp-detection-clarity-09]
---

# Энциклопедия и совет узнают про тревогу базы

## Goal

После story в «📖 Пути новичка» есть раздел «Нападение на базу», объясняющий, откуда берётся тревога,
какие три хода есть у защитника и что окно конечно; а «Совет дня» получает новую запись категории
«бой», которая рассказывает про это же тому, кто в справочник не заходит.

## Requirements

> Логика и описание понимания в процессе встречи, атаки — всё должно быть учтено и в коммуникации должно быть видно

> То есть в этом и будет суть защиты базы.

## Files
- app/Services/Onboarding/GuideCatalog.php
- app/Database/Migrations/2026-09-12-000000_Adr186SeedStandoffTip.php
- tests/database/StandoffContentTest.php

## Non-goals
- Не переписывать разделы, которые правила story `-05` (`settings`, `arena`): здесь добавляется новый раздел, а соседние трогаются только если после `-05` осталось прямое противоречие — и тогда об этом сказано в отчёте.
- Не вписывать в тексты длительность окна, кулдаун и проценты: числа живут в админке, дублирование в тексте — гарантированный дрейф.
- Не давать за просмотр раздела ни наград, ни выдач, ни телепортов: `/guide` — read-only, это проверяется source-scan тестом каталога.
- Не заводить второй совет и не дублировать существующий `FieldPvpAndArena`.
- Не запускать полный набор и не делать `DROP`/`migrate` на общей локальной тест-БД; не делать `git stash`/`git checkout`.

## Map slice
`app/Services/Onboarding/GuideCatalog.php:672-689` (раздел `arena` — образец тона и соседний по теме);
ADR-186 §3, §8 и раздел «Ворота проекта» (вердикты Редколлегии);
правила GUIDE-coverage и TIPS-coverage в `CLAUDE.md`; `## Contracts` плана (имена кнопок, чтобы текст
называл их точно так же, как их видит игрок).

## Acceptance criteria
- [ ] В `GuideCatalog` появился раздел про нападение на базу: что такое тревога, при каком условии она поднимается (живая оборона на своей клетке), три хода защитника, что окно конечно и после него бой состоится, и что тревога — разовое право на реакцию, а не постоянный щит.
- [ ] Инварианты `/guide` соблюдены: ключ раздела только из `[a-z]` без `_`; текст read-only, самодостаточный в media-off, парные `*`; речь про навигацию и понятия, а не про числа баланса.
- [ ] Названия кнопок в тексте совпадают с теми, что реально приходят игроку в сообщении тревоги (сверено с кодом `-09`, а не с планом по памяти).
- [ ] Совет засеян идемпотентной миграцией по уникальному `title_en = BaseStandoffAlert`, категория `бой` (одна из 14 допустимых), тон Роби, эмодзи безопасны, markdown парный; повторный прогон миграции не плодит дублей.
- [ ] Совет не обещает того, чего нет при выключенном килсвитче: формулировка верна и когда `pvp.standoff.enabled = false` — либо совет засеивается вместе с включением, и это названо в отчёте.
- [ ] Тест доказывает: раздел существует и отдаётся каталогом, ключ валиден, миграция идемпотентна по `title_en`, категория входит в ENUM.
- [ ] В `## Implementation notes` сказано: правдивость и тон текста тестом не проверяются — их выносит Редколлегия, а рендер в Telegram доказывается Tier-3 (`/guide` и `/tips` на testbot'е).

## Verification
`vendor/bin/phpunit --no-coverage --no-progress tests/database/StandoffContentTest.php`
`git ls-files 'app/Database/Migrations/*.php' | xargs -n1 php -l > /dev/null`

## Implementation notes

## Findings
