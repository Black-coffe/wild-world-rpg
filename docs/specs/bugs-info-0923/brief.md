# Brief — bugs-info-0923

**Date:** 2026-09-23
**Tier:** 3
**Escaped from:** — (независимые баг-репорты игроков, не одна зашипленная спека)

## Request (verbatim)

Владелец, 2026-09-23:

> Брешь Google-ом MCP браузер, чи під браузер відкриваєш Telegram-чат із групою, де гравці сидять, відкриваєш групу з багами і продивляєшся всі баги, на які ми не давали відповіді. Якщо ці баги в грі є, береш їх всі, плануєш через вулик-план і запускаєш починку, ремонт цих багів, фіксинг з смок-тестом на препроді і потім деплоєм на прод. Це твоя задача на сьогодні.

## Источник — неотвеченные баг-репорты, Wild World Info → Bugs-info (дословно, прочитано 2026-09-23)

1. Анжела, 2026-09-03 19:10–19:11, mid 4294974600–602:
> Ладно, игру я прошла, последний баг-репорт:
> Ордината 0 не отображается

   Скриншот: экран карты после «Вы двинулись на: юго-запад», «Игрок по центру (X=995, Y=1)»; верхние ряды сетки — белые клетки.

2. Анжела, 2026-09-11 08:16, mid 4294974617:
> В крафтовых предметах есть еда, которая не отображается в аптечка-провизия

   Скриншот: «🍲 Еда 🍲 / 📦 Мясо дикого кабана | 2 шт.», ниже кнопки «Рыбные консервы», «💊 Аптечка (7)».

3. Torch0010, 2026-09-17 09:56, mid 4294974737:
> Этот блок как то некорректно работает - тыкоешь на кнопку, сообщение заново приходит, такое же, без описания квеста

   Скриншот: список квестов «… Выберите квест 👇, чтобы получить подробную информацию» с клавиатурой названий квестов («Изучить 30 ячеек», «Изучить все биомы», «Крафт аптечки баз…», «Захват Бункера», …).

4. Евгений Лисовский, 2026-09-17 11:55, mid 4294974738:
> 📦 Добытые ресурсы:
> — Crops: 3 шт.
> — Fruit: 2 шт.
> — Wood: 6 шт.
> — Clay: 29 шт.
> — Water: 6 шт.
> нет перевода на старой ферме

5. Евгений Лисовский, 2026-09-18 12:10, mid 4294974739:
> "надеть"

   Скриншот: карточка брони с кнопками «Одеть», «💰 Продать торг…», «↩ Назад».

Не баг, вопрос (ответить в ветке, не чинить): Анжела, 2026-09-15, mid 4294974729 — «Теперь можно будет запускать несколько роботов промышленников одновременно? По одному на каждую базу?»

## Разведка (Queen + 2 drone-scout, 2026-09-23; данные — preprod-testbot)

- **(1) Y=0:** `map` на testbot — плотная сетка 0..999 × 0..999 (по 1000 клеток на `coordinate_y=0` и на `coordinate_x=0`); движение (`MoveCharacterToDirectionAction` `MAP_MIN=0`) и текстовая сетка (`TextMapService.php:223`) держат 0 внутри мира. Картинка изученного `ExploredMapService.php:149-152` зажимает окно в `max(1, …)` / `min(WORLD_SIDE=1000, …)` — мир считается 1..1000, нулевые строка и столбец не рисуются никогда.
- **(2) Еда:** `crafted_items` id 23/24/25 (`Мясо дикого кабана`, `Рыбный суп`, `Ягодный джем`) — `type='food'`, `status='active'`; `CraftedResourcesAction.php:148` показывает их под «🍲 Еда», `ProvisionAction.php:49`/`PharmacyAction.php:44` читают только `type='drug'`. ADR-185 решил их вывести, но ветка `vulyk/craft-shelf-coverage` не влита в `develop`.
- **(3) Квесты:** `Quest/QuestsInfo.php` — кнопки `questInfo_<title_en>`, карточки захардкожены только для 4 квестов (`Explore30Cells`, `ExploreAllBiomes`, `Explore300Cells`, `FirstAidkitBasic`), остальные падают обратно в список. `quests` несёт `title_ru`, `description`, `min_level`, `reward_type`, `reward`, `prerequisite_quest`, `faction_id`.
- **(4) Ферма:** `StrategicLootHandler.php:181-186` — `ResourceModel` возвращает `ResourceEntity`, проверка `is_array($resRow)` ложна → печатается сырой ключ. Ключи `S24AddIslandFarm` (`Crops`, `Fruit`, `Wood`, `Clay`, `Water`) совпадают с `resources.name_en` (id 45, 48, 2, 13, 17). Кандидаты-двойники того же класса: `Camp/Buildings/Greenhouse/PlantCropActionStart.php:72`, `Camp/Buildings/Robots/RobotRepairConfirmAction.php:69`, `Drone/CargoDroneSendAction.php:109`, `Services/Player/BuildingUpgrade/BuildingUpgradeValidator.php`.
- **(5) «Одеть»:** `Profile/GearArmorDetailAction.php:187`, `Profile/GearWeaponDetailAction.php:208`; роутинг по префиксу `callback_data` (`CallbackRoutes.php:236,238`), не по тексту.

## Answers

1. **Еда** — Честная пометка: на экране крафтовых предметов у трёх food-предметов пометка «не применяется, выводится из обращения» и путь к живой провизии; вывод с компенсацией остаётся за `craft-shelf-coverage`.
2. **Ответы** — Да, реплаями после прода: после прод-смока ответ реплаем на каждый из 5 багов и на вопрос Анжелы про роботов с аккаунта Main, с номером версии.
3. **Требования** — Верно, строим (straight-through по `--go` владельца).

## Asks

1. Картинка изученной карты (`ExploredMapService`) рисует мир 0..999 по обеим осям: изученная клетка с `x=0` или `y=0` попадает в кадр и закрашивается, окно не выходит за 999; тест с изученными клетками (0,0) и (999,999) доказывает границы окна. Текстовая сетка вокруг игрока на `Y=1` рисует ряд `Y=0` как клетку мира (не «⬜ за краем») — тест.
2. Экран «Крафтовые предметы»: у каждого предмета `type='food'` (Мясо дикого кабана, Рыбный суп, Ягодный джем) рядом с именем видна пометка, что он не применяется и выводится из обращения, и строка с путём к живой еде «🎒 Инвентарь → 🥣 Провизия»; текст полон без картинки, без чисел баланса; остальные типы экрана не меняются.
3. Список квестов (`QuestsInfo`): тап по кнопке ЛЮБОГО квеста из таблицы `quests` открывает его карточку — название, описание из БД, минимальный уровень, награда, предшествующий квест если есть — с кнопками «назад к списку» и «◀️ Я»; кнопки из старых сообщений (`questInfo_<title_en>`) тоже открывают карточку; неизвестный квест → честный отказ с кнопкой назад, а не молча тот же список; `callback_data` ≤64 байт — тест на квест вне прежних четырёх.
4. Сообщение «🏛 Найден стратегический объект!» (`StrategicLootHandler`) называет ресурсы по-русски (`resources.name`) — для Старой фермы «Зерновые культуры / Фрукты / Древесина / Глина / Вода»; тест с настоящим `ResourceEntity`. Каждый кандидат-двойник из разведки проверен: читает строку `ResourceModel` правильно, либо исправлен с тестом; вердикт по каждому записан в story.
5. Кнопка экипировки на карточках брони и оружия — «Надеть» (роутинг не меняется); `git grep -n "Одеть" -- app` пуст.
6. Гейты зелёные: `vendor/bin/phpunit --no-coverage --no-progress` (на свежей пустой тест-БД), `vendor/bin/phpstan analyse --memory-limit=512M --no-progress`, `php -l` миграций; tech-writing ноты в `mmorpg-vault/tech-writing/` обновлены для каждого тронутого класса. Вердикты: `/guide` — нет (фиксы без новой механики и без нового входа); tips — нет (то же); онбординг — нет нового шага; discoverability — новых входов нет, пометка еды сама ведёт к Провизии.
7. Живой Tier-3 на preprod-testbot (тест-чар `telegram_user_id=25`, вебхук или Telegram Web): карточка квеста вне прежних четырёх открывается; карточка брони показывает «Надеть»; «Крафтовые предметы» с food-предметом показывают пометку; принудительная находка Старой фермы пишет ресурсы по-русски. Результат записан в `plan.md`.
