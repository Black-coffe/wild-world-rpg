<!-- seat: review · model: unknown · round: 1 · head: 75717c8b · pack: 1803240603db · attempt: 1 · recorded: 2026-09-23T14:41:20Z · verdict: PASS -->
VERDICT: PASS

Adversarial review: bugs-info-0923, round 1. Diff 0fb076ea..75717c8b (branch vulyk/bugs-info-0923).

## Scope
Every code file in the diff belongs to a story's `## Files`. The one crossing is `phpstan-baseline.neon`: story 03 edited it (10 entries for QuestsInfo) although it is listed only under story 04. The `## Plan deltas` entry in plan.md records this. The 13 removed baseline entries all belong to QuestsInfo.php (10) and StrategicLootHandler.php (3). No Law-3 violation beyond that recorded one.

## Critical
None.

## Major
1. brief.md Ask 2 and plan.md `## Contracts` (story 02); code at app/Controllers/Telegram/Commands/Actions/CraftedResourcesAction.php:75. `plan`. The brief and the contract must name the path line the screen actually shows, or record the change as a plan delta. The delivered line is "↳ Еда и питьё, которые работают: 💊 Аптечка → 🍲 Провизия", not "🎒 Инвентарь → 🥣 Провизия". The worker was right: 🥣 does not exist, and PharmacyAction.php:60/97 carries "🍲 Провизия". But the deviation is only in the story's Implementation notes. The story asked for INTERFACES, and the brief, the Contracts and the Tier-3 smoke substring still expect the invented text. The council judges the brief literally.
2. brief.md Ask 5 and the plan.md Integration gate (story 05). `plan`. Ask 5 and the gate must match what the Queen delta actually permits. `git grep -n "Одеть" -- app` is not empty and cannot become empty under the chosen approach. It returns the old seed at app/Database/Migrations/2026-10-24-100000_SeedArmorScreenTip.php:42 and 3 lines in the new migration itself, app/Database/Migrations/2026-12-08-100000_FixArmorScreenTipNadet.php:11,21,29. The Queen delta sits only inside the story file, which the blind seats never read.

## Minor
3. docs/specs/bugs-info-0923/bugs-info-0923-05-nadet-label.md, Implementation notes, "git grep … находит только строку внутри старого сида". `worker`. This verification claim must match the tree. The same grep also hits the story's own new migration (3 lines: :11, :21, :29).
4. docs/specs/bugs-info-0923/bugs-info-0923-02-food-marker.md, Non-goals/AC naming `CraftedItemTypeApplicationCoverageTest`. `plan`. The story must name a guard that exists. That test is nowhere in the repo; the only neighbour is tests/unit/Display/CraftedItemTypeHeadingCoverageTest.php. The worker noted this, but no plan delta records it.
5. brief.md "Разведка (1)" and Answers 2 (reply to Angela). `plan`. The player reply must not claim that this release fixes the symptom she reported. Her screenshot shows the text grid on the move screen (MoveCharacterToDirectionAction.php:720, "Игрок по центру"). Its Y=0 ⬛️ cause (the `revealAround` 1..1000 clamp) was already fixed by 051b36af "fix(карта): ряд Y=0 и столбец X=0 раскрываются…" plus the backfill 2026-12-06-100000_BackfillExploredCellsWorldEdgeZero.php, shipped in v0.51.655 on 2026-09-03. This spec fixes the sibling clamp in the explored-map image. That fix is real and correct, but it is not the one the reporter saw.
6. tests/unit/Craft/CraftedResourcesFoodMarkerTest.php:137-146 (dropTables in setUp/tearDown). `plan`. The test must not leave the shared `wildworld_tests` without `crafted_items`/`crafted_items_log` for later tests. It drops both tables unconditionally and never restores them. Compare tests/unit/Telegram/QuestInfoCardTest.php:57-69, which drops only what it created. The repo has the same idiom in tests/database/StrategicLootHandlerEntityContractTest.php, and the suite ran green under close-story, so this is an order-dependence risk, not a current failure.
7. tests/unit/Services/World/MapZeroEdgeTest.php:160-205, TextMapService doubles. `plan`. The text-grid pin must exercise the coordinate range filter, or state that it does not. The MapModel/ExploredCellsModel doubles ignore every `where()` except `cell_number`, so a regression in TextMapService.php:107-111 (for example `coordinate_y >= 1`) would stay green. The `$worldY < 0` check at TextMapService.php:223 is covered.
8. tests/unit/Telegram/QuestInfoCardTest.php (whole file). `worker`. The card path should be proven through `handle()` as well as through the static `cardTail()`/`buildCard()`. The wiring at QuestsInfo.php:48-53 (callback data → tail → card instead of list) is untested. Reverting just those lines would bring back the original "same list again" bug with every test still green.
9. app/Controllers/Telegram/Commands/Actions/Quest/QuestsInfo.php:172-175. `worker`. A quest with `min_level = 0` should not read "доступен с 0-го уровня". Only NULL takes the no-restriction branch.
10. app/TaskHandlers/Objects/StrategicLootHandler.php:186. `worker`. A `resources` row with NULL `name` must fall back to the raw key. The guard `$resRow->name !== ''` lets null through and prints an empty `**`. The column is NOT NULL in the test's temp schema, so the test cannot see this.
11. docs/specs/bugs-info-0923/plan.md. `plan`. plan.md must have one `## Plan deltas` section. There are now two: the cycle-state lines sit under the first, and the Queen's baseline delta sits under the second.

## Checked and clean
- ExploredMapService: `window()` clamps to 0..999 and the pixel offset is `(x-min)*cellPx`. The worker's claim that "all 3 image tests go red on the old clamp" holds for each of the three when traced by hand: (a) min_x becomes 1; (b) cell (0,0) is outside a 1..1000 window, so pixel (0,0) stays unopened; (c) min_x becomes 1. No other 1..1000 world clamp remains in app/Services or the Telegram actions.
- Story 04 twins: I spot-checked all four verdicts against the code and they hold. PlantCropActionStart:72 uses a raw builder with getRowArray. RobotRepairConfirmAction:69 uses CharacterResourceModel with returnType array. CargoDroneSendAction:109 uses a raw query with getRowArray. BuildingUpgradeValidator:180 calls `->toArray()` first. The crafted-items branch right below the fix uses CraftedItemsModel with returnType 'array', so it is clean. The fix plus the test with a real `ResourceEntity` would go red on the old `is_array` path.
- Story 03: QuestModel returnType is 'array'. `questInfo_*` resolves through the exact route on explode('_')[0] (CallbackRoutes.php:303, :593). `^id(\d+)$` matches the contract, and the query builder binds `title_en`, so there is no injection. On local data, the DB descriptions of the four legacy quests still carry the facts the hardcoded cards had, including the Explore300Cells "+2 stats" (QuestExplore300CellsHandler.php:65-68), so the legacy cards do not regress.
- Story 05: `callback_data` is unchanged. The migration passes `php -l`, its 2026-12-08-100000 prefix is unique, and there is no new table or column, so WipeManifest is not needed.
- Security: nothing new to flag. There are no new endpoints, and all DB lookups on callback input are parameterized.
- Reinvention: none. The escaping in story 03 reuses `App\Services\Display\MarkdownSafe`.

Commands run: `git diff` and `git grep` inspection; `php -l` on the new migration; read-only SELECTs on the local `mmorpg` DB (quests rows). I did not re-run the suite.
