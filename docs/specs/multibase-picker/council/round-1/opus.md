<!-- seat: opus · model: claude-opus-5 · round: 1 · head: 981a1b56 · pack: e51476922063 · attempt: 1 · recorded: 2026-09-15T10:27:01Z -->
COUNCIL: multibase-picker · round 1 · seat opus
MODEL: claude-opus-5
COURT: C:/laragon/www/mmorpg/.vulyk/court/multibase-picker/round-1
VERDICT: RED
ASSUMED CONFIG: Local (Laragon) code judged by reading it plus a stubbed probe with no DB. Preprod-testbot and prod were not touched.
RAN: grep/sed/cat on app/ and tests/ in COURT; one scratch PHPUnit probe (scratchpad/ResolveProbeTest.php, stubbed ClaimedCellModel + CommunicationTowerCoverageService) run via vendor/bin/phpunit --no-coverage --no-progress on that single file.
PATH: none walked live (Browser MCP = none; the Telegram door is the Queen's manual Tier-3). Walked in code: Base -> picker -> Base_b<id> -> construction/building/hangar/baseDevelopment/campDecor -> AllRobots -> startRobotGatherer -> CompleteRobotGatheringHandler.
ASK 1: GREEN - picker for >=2 bases off-base - run: sed BaseService::showBaseInfo/showBasePicker, BaseServiceMessageFormatter::basePicker saw: on own base -> findActiveCell screen; exactly 1 covered -> showBaseBuildings directly; otherwise covered bases become `Base_b<id>` buttons, the rest are text with name, X/Y and "расстояние: N ходов", plus a Телепорт/Двигаться row (BasePickerTest covers 2/0/1 covered)
ASK 2: GREEN - per-base coverage + GameSettings radius - run: cat CommunicationTowerCoverageService + 2026-12-07-100000_SeedTowerCoveragePerLevelSetting saw: coverageByBase over findAllActiveCells (id ASC), tower matched to the base cell (map_cell_id); `communication_tower.coverage_per_level` default 100, rationale/effect/above/below, soft 50-200, hard 1-1000, default_value_text '100'
ASK 3: GREEN - base id in callback_data + re-check - run: grep BaseCallbackSuffix app/; read BaseScopeResolver::resolveForBase, BaseCallbackSuffixRoutingTest saw: suffix on picker, construction, building cards, upgrade, hangar, baseDevelopment, campDecor; append() throws past 64 bytes (test at 64/65, longest name + 10-digit id); resolveForBase checks own+active+on-base-or-own-tower; probe resolveForBase(base1 uncovered)=unavailable; no suffix -> resolve()
ASK 4: GREEN - Hangar of that base - run: sed HangarAction saw: workshopAtBase(char, baseCell) gives that base's level, baseLabel = name + (x, y); locked text "нужна Мастерская робототехники на этой базе" + path 🏠 База -> 🏗 Строить -> 🤖 Мастерская; no max() across bases
ASK 5: RED - robot launches at the selected base - run: scratch probe BaseScopeResolver::resolve(), off-base, only base-2's tower covers saw: "PROBE resolve() cell=1000 (base-2 cell is 2000, base-1 cell is 1000)". StartRobotGatheringAction:148 and RobotGathererActivator:74 use resolve() (not the base suffix) and AllRobots/startRobotGatherer carry no base. Off-base under base-2's signal only, launch picks base-1: it refuses naming base-1 if base-1 has no Workshop, or digs around base-1, a base the player is not under. Ask 8's own path "Ангар второй базы -> запуск робота называет базу" names the wrong base. base_cell in task_settings and the completion fallback are fine; tests cover only on-base launches.
ASK 6: GREEN - development/decor scoped; ADR-187 note - run: sed BaseDevelopmentAction, grep BaseCampDecorAction saw: MAX(level) filtered by cb.map_cell_id = selected cell; decor setters take $cell from resolveForBase. ADR-187 lives in mmorpg-vault, outside COURT, so that half is not verified
ASK 7: GREEN - media-off/discoverability/tips/guide - run: cat SeedMultibasePickerTip; grep GuideCatalog saw: tip is idempotent by title_en, tip_type 'общие', Robi tone, no balance numbers, paired *; guide 'base' section line 317 "🗼 Несколько баз — выбор по сигналу"; picker, hangar and launch captions carry name/coords/distance/level in text; entry is still 🏠 База
ASK 8: N/A - why: the suite and phpstan are the sonnet seat's angle; the tech-writing notes sit in mmorpg-vault outside COURT; live Tier-3 needs the manual testbot smoke (Browser MCP = none)
UNASKED: (a) BaseScopeResolver::resolve() returns activeCells[0] whenever ANY tower covers, even when only another base's tower does. Every legacy/no-suffix path (old buttons, the robot launcher, the bare "🏠 База" back buttons) can therefore land on a base the player is not under. Fixing it to "the first COVERED base by id" would fix ask 5 as well. (b) The "🏠 База" back buttons on building cards and robot screens, "🏗 К базе" in Развитие, and "🏗 Развитие базы" on the LeanTo card are bare, so from a remote second base they drop back to the picker or base-1 rather than the base in hand. (c) Hangar lock "🏗 Строить" -> Build_b<id> routes to BuildListAction, which ignores the base; harmless, since building happens on the current cell anyway.
BREACH: none
