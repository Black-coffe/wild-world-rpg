# Brief — multibase-picker

**Date:** 2026-09-15
**Tier:** 3
**Escaped from:** angela-second-base-bugs (ADR-187 «Revisit when: владелец захочет выбор базы»; `plan.md` `## Открытые хвосты`)

## Request (verbatim)

Владелец, 2026-09-15 (после `/clear`):

> Подхватывай из handoff задачи, запускай в улик план и приступай к реализации.

Владелец, 2026-09-14, на вопрос о варианте для багов по базам (варианты — `docs/specs/player-bugs-2026-09-14/report.md`, «Варианты по базам»):

> Давай на твое усмотрение

Выбран вариант 1 — «Добить мульти-базу выбором базы». Анжеле в треде Bugs-info обещано «воспроизвели, причину нашли, чиним, отпишемся».

## Источник — баг-репорты Анжелы, ветка Bugs-info, 2026-09-14 (дословно, `docs/specs/player-bugs-2026-09-14/thread.md`)

1. mid 4294974700, 12:44:
> #bug 
> Отхожу от второй базы на один шаг, жму «база», вижу интерфейс только первой базы, интерфейса второй базы, на которой тоже есть вышка связи и от которой я в одном шаге - не вижу! 

   Скриншот: «Вы сейчас не физически на базе, но сигнал Вышки связи (ур. 10) покрывает 727 ходов … Координаты базы: x=767 y=752» — это база-1.

2. mid 4294974704, 13:02 (предложение):
> КМК на карте нажатие на кнопку «база» должно выводить кнопки баз, вышки связи которых дотягиваются до перса, а уже по нажатию на них можно проваливаться в интерфейс управления конкретной базой 

3. mid 4294974705, 13:05:
> #bug 
> 
> Стою на второй базе, ангар на ней не построен, но кнопка есть, по нажатию проваливаюсь в интерфейс ангара первой базы 

4. mid 4294974709, 13:07 и mid 4294974712, 13:08:
> #bug 
> 
> Стоя на второй базе без ангара запустила робота промышленника 

> В какой локации - непонятно 

   Скриншот: «робот привязан строго к координате твоей базы» — без указания, какой.

## Разбор (из study `player-bugs-2026-09-14/report.md`, проверить разведкой)

- «База»: `CommunicationTowerCoverageService::checkCoverage()` знает одну базу (`first()` без `orderBy`); `BaseScopeResolver::resolve()` под вышкой берёт первую активную по `id` — сознательно (ADR-187).
- «Ангар»: хаб автоматизации (ADR-120), кнопка всегда видна намеренно; `HangarAction` ищет мастерскую и роботов по `character_id` без базы.
- Робот: `StartRobotGatheringAction` / `RobotGathererActivator` берут мастерскую/базу по `character_id` + `first()`.
- Прецедент выбора базы кнопками — `backpack-teleport-base-choice` (ADR-102, `TeleportUseValidator`).

## Answers

1. **Память базы** — База в самой кнопке: номер базы добавляется в callback кнопки постройки (как в выборе базы телепорта); кнопки из старых сообщений продолжают работать по старому правилу; хранилище не нужно.
2. **Кого в выбор** — Под сигналом — кнопки, остальные — текстом: базы, до которых дотягивается Вышка связи, — кнопками (как просила Анжела); остальные перечислены в тексте с координатами и расстоянием в ходах, плюс «Телепорт/Двигаться», как сейчас.
3. **Робот** — Работает у выбранной базы: запуск только с базы, где есть своя Мастерская робототехники; робот копает вокруг этой базы (база запоминается в задании, без новой колонки); экран запуска называет базу и координаты; роботы остаются общим инвентарём персонажа.
4. **Похожие места** — Развитие и декор — да, маяки — в хвост: «Развитие базы» и «Декор базы» работают с той же базой, что и пикер; правило «Центр телепортации на любой базе» у маяков записывается как намеренное, не чинится.
5. **Требования** — Верно, покажи план до сборки.

## Asks

1. «🏠 База» у персонажа с ≥2 активными базами, стоящего НЕ на своей базе: каждая база, до клетки игрока от которой дотягивается её собственная Вышка связи, — отдельной кнопкой; остальные активные базы перечислены в тексте сообщения с именем, координатами и расстоянием в ходах, под ними — «Телепорт»/«Двигаться». Ровно одна база под сигналом → её экран открывается сразу, без выбора. Игрок стоит на своей базе → экран именно этой базы.
2. `CommunicationTowerCoverageService` оценивает покрытие по КАЖДОЙ активной базе персонажа в детерминированном порядке (по `id`), а не по одной `first()` без `orderBy`; радиус покрытия на уровень вышки — ключ `GameSettings` с rationale/effect/above/below, soft/hard-границами и Reset-to-default (сейчас зашито `towerLevel * 100`), дефолт равен нынешнему поведению.
3. Кнопка базы в пикере и все кнопки, ведущие с экрана выбранной базы (карточки построек, «Поднять уровень», «🤖 Ангар», «Развитие базы», «Декор базы»), несут идентификатор базы в `callback_data` (≤64 байт, проверено тестом); обработчик заново проверяет, что база принадлежит персонажу, активна и доступна (игрок на ней или под её сигналом), иначе — честный отказ. Кнопка без идентификатора (из старых сообщений) работает по прежнему правилу `BaseScopeResolver`.
4. «🤖 Ангар» показывает Мастерскую робототехники и её уровень ТОЙ базы, с которой открыт, и называет базу (имя + координаты) в тексте. Нет Мастерской на этой базе → lock-состояние с объяснением «нужна Мастерская робототехники на этой базе» и путём к постройке; хозяйство другой базы не показывается как местное.
5. Робот-промышленник запускается только с базы, на которой есть своя Мастерская робототехники; клетка базы запуска сохраняется в задании (`character_tasks.task_settings`), и завершение сбора (`CompleteRobotGatheringHandler`) собирает вокруг именно неё. Экран запуска и сообщение-итог называют базу и координаты. Задание, запущенное до выкатки (без сохранённой базы), завершается без ошибки по прежнему правилу.
6. «Развитие базы» показывает уровни построек выбранной базы, а не `MAX(level)` по всем базам; «Декор базы» правит выбранную базу. Правило маяков («Центр телепортации на любой базе») записано как намеренное в обновлении ADR-187, где закрыт пункт «Revisit when: владелец захочет выбор базы».
7. Media-off: пикер, экран базы, Ангар, запуск и итог робота полны без картинки — имя базы, координаты, расстояние, уровень, состояние и инструкции несёт текст. Discoverability: вход — прежняя кнопка «🏠 База», новых скрытых входов нет. Onboarding: JIT-подсказка не нужна — экран выбора сам объясняет, что баз несколько. Tips: да — идемпотентная seed-миграция `*Seed<Что>Tip.php`, категория `общие`, тон Роби, без чисел баланса. `/guide`: да — абзац о нескольких базах и Вышке связи в разделе `base` `GuideCatalog`.
8. Гейты зелёные: `vendor/bin/phpunit --no-coverage --no-progress` и `vendor/bin/phpstan analyse --memory-limit=512M --no-progress`, `php -l` миграций; tech-writing ноты в `mmorpg-vault/tech-writing/` обновлены для каждой тронутой сущности. Живой Tier-3 на preprod-testbot персонажем с двумя активными базами под сигналом: выбор базы → карточка постройки второй базы → «🤖 Ангар» второй базы → запуск робота называет базу.

## Как проверять (для совета)

Полный набор на общей `wildworld_tests` красный и без правок (FK `resources_bank`, `r.weight`) — это состояние машины, не регресс. Гонять на свежей пустой БД:

```
mysql -uroot -e "DROP DATABASE IF EXISTS wildworld_ci_mbp; CREATE DATABASE wildworld_ci_mbp CHARACTER SET utf8mb4;"
env "database.tests.database=wildworld_ci_mbp" vendor/bin/phpunit --no-coverage --no-progress
```
