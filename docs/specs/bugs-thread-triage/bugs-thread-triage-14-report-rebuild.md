---
story: bugs-thread-triage-14
spec: bugs-thread-triage
status: todo
tier: 2
worker: worker-code
tracer: false
wave: 6
blocked_by: [bugs-thread-triage-10, bugs-thread-triage-11, bugs-thread-triage-12]
---

# Пересборка отчёта после ремонта

## Goal

`REPORT.md` собран из файлов вердиктов, которые ремонтный круг изменил: критический черновик про
транспорт переписан, три навигационных черновика исправлены, один вердикт сменил ярлык, три
доказательства сужены. Отчёт обязан описывать починенную стопку, а не прежнюю. Плюс две
собственные находки ревью по самому отчёту.

## Requirements

> Если да, пишешь ответ, что данная система устранена, бага нет.

> смотришь: мы эту систему поправили, баг устранили?

## Files
- docs/specs/bugs-thread-triage/REPORT.md
- docs/specs/bugs-thread-triage/bugs-thread-triage-08-report.md

## Что делать

1. **Пересобери отчёт** из текущих `verdicts/*.md`. Структура прежняя (шапка с числами и линией
   отсечения, часть 1 к отправке дословно, часть 2 владельцу, раздел «Публикация»).
2. **Числа пересчитай заново.** Один вердикт переехал из `не баг` в `подтверждено, но не дефект
   кода` — в чат по нему по-прежнему ничего не уходит, но в таблице шапки он теперь в своей строке.
   Возьми верную цифру невыкаченных коммитов из `RECON.md` (её исправила story 09) — **40**, не 60.
3. **Висячая ссылка (находка ACC-1).** Строка про хвост Tоrch0010 (`4294974587` — довайповый робот,
   который нельзя скрафтить заново) отправляет читателя «см. часть 2», а в части 2 этого пункта нет.
   Открытый пункт молча выпал из списка «что чинить». Добавь его в часть 2 или убери отсылку —
   но не оставляй ссылку в никуда.
4. **Диапазон дат назван двумя способами (MINOR-13).** Шапка отчёта и `DATA.md` говорят
   «2026-05-29 → 2026-09-12», а `thread.md` — «2025-01-14 → 2026-09-12», и закреплённые правила
   ветки (`4294971814`, 2025-01-14) входят в число 81. Приведи к одному.
5. **Заметки story 08.** Заполни в `bugs-thread-triage-08-report.md` разделы
   `## Implementation notes` и `## Findings`, включая то, о чём просил её собственный Non-goal:
   `Write` отказался писать по пути с «report» в имени, файл пришлось класть через `cp` из
   скретчпада.

## Non-goals
- Не редактировать `verdicts/*.md` — они принадлежат story 10–12.
- Ничего не отправлять в Telegram. Публикация — работа Queen'а после «ок» владельца.
- Не пересматривать вердикты и не «дожимать» сомнительные до `устранено`.

## Map slice
не нужен — источник в `verdicts/*.md` и `RECON.md`

## Acceptance criteria
- [ ] Отчёт пересобран из текущих вердиктов; ни один `mid` не потерян.
- [ ] Числа шапки пересчитаны; невыкаченных коммитов — 40.
- [ ] Хвост `4294974587` либо в части 2, либо отсылка на него убрана.
- [ ] Диапазон дат назван одинаково в отчёте и в `DATA.md`.
- [ ] Разделы заметок story 08 непусты.

## Verification
`test -s docs/specs/bugs-thread-triage/REPORT.md && test -z "$(comm -23 <(grep -ho '[0-9]\{10\}' docs/specs/bugs-thread-triage/verdicts/*.md | sort -u) <(grep -o '[0-9]\{10\}' docs/specs/bugs-thread-triage/REPORT.md | sort -u))"`

## Implementation notes

## Findings
