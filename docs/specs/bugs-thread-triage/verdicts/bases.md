<!-- mids: 4294974283 4294974469 4294974508 4294974616 4294974671 4294974674 -->

### `4294974283` — 2026-05-29 — Morginwara

**Суть:** «когда хочу выбрать постройки, чтобы построить — не нажимается»; уточнение `4294974291` — персонаж Feather, постройка «центр телепортации»; ответ владельца `4294974284` («напиши что ты пытался построить») остался без ответа игрока.
**Вердикт:** не воспроизводится
**Чем доказано:** grep по `thread.md` — по этому mid ровно три сообщения (`4294974283`/`4294974284`/`4294974291`), дальше ни «Morginwara», ни «Feather» в ветке не встречаются — подтверждения и опровержения починки нет. Прочитан целиком `GenericBuildingInfoAction::handle()` — на каждом отказе (нет базы, не на базе, низкий уровень, не хватает ресурсов/предметов) отправляется явное сообщение с кнопками, а не тишина; `TeleportationCenter` в `Config\Buildings.php` (`level_required => 12`) идёт тем же путём — «не нажимается» этим кодом не воспроизводится. Отдельно позже (`d8e86c7a`, 18.06, тег ≤ v0.51.666) закрыт весь класс «тап по устаревшему/нераспознанному callback → тишина» алертом в `CallbackqueryCommand`, но привязать его к этой конкретной жалобе нечем — жалоба датирована раньше и не содержит деталей клавиатуры.
**На проде:** прод (v0.51.666) не воспроизводит тишину на тап ни в одном просмотренном пути постройки центра телепортации — вывод из чтения кода, SELECT не требуется.
**Живой прогон:** не нужен
**Черновик ответа:** —
**Сомнения:** два коротких сообщения без деталей клавиатуры/скриншота и без дальнейшей переписки — нельзя исключить, что дело было в конкретной Telegram-сессии игрока (сеть, устаревшая клавиатура после рестарта бота), а не в игровом коде; текущий код по всем найденным путям отвечает, а не молчит.

### `4294974469` — 2026-07-21 — Kinder surprise

**Суть:** построил Верстак 1 трижды — постройки, у которых он в зависимостях (`Config\Buildings.php`: Теплица/Спортзал/Солнечная станция/Мастерская робототехники/Центр телепортации требуют `crafted_items.WorkbenchOne => 1`), «не видят» его. Анжела (`4294974472`) подсказала смотреть в Инвентарь → Крафтовые ресурсы; игрок ответил «попробую ещё раз построить» (`4294974474`) и на этом переписка обрывается.
**Вердикт:** требует живого прогона
**Чем доказано:** прочитан весь путь целиком. Прод-SELECT: `crafted_items` содержит `WorkbenchOne` (`id=80`, `name_rus='Верстак 1го уровня'`) — таблица не пустая и не рассинхронена. `GenericBuildingInfoAction::checkCraftedItems()` резолвит требование по `name_eng` через `crafted_items`+`crafted_items_log` — на вид корректно; `GenericCraftCompletionHandler` пишет `crafted_items_log` при завершении крафта — на вид тоже корректно. Отдельно найден дефект, НЕ объясняющий эту жалобу: легаси `ArmorDrifterClothes2Action`/`ArmorRaggedShirt2Action`/`ArmorLeatherJacket2Action`/`ArmorReinforcedLeather2Action` проверяют «Верстак 1» через `buildingModel`/`characterBuildingModel` (`buildings`+`character_buildings`), а прод-SELECT показывает, что строки `name_en='WorkbenchOne'` в `buildings` нет вовсе — значит этот чек всегда молча пропускается (слишком мягкий, а не слишком строгий, жалобе не соответствует). Персонажа Kinder surprise найти не удалось: SELECT по `telegram_users.first_name`/`username` с маской «kinder» — 0 строк, посмотреть его личный `crafted_items_log` нечем.
**На проде:** воспроизводимость на данных реального игрока не проверена — SELECT по неизвестному `character_id` невозможен, статический разбор кода дефекта в актуальном пути не находит.
**Живой прогон:** нужен. Предусловие — тест-чар на testbot без Верстака 1 в инвентаре (`SELECT` по `crafted_items_log` подтвердить 0 строк для `crafted_item_id=80`), уровень ≥1, есть база. Путь — 🔧 Мастерская → крафт «Верстак 1» → дождаться завершения задачи → Инвентарь → Крафтовые ресурсы → Верстаки (убедиться, что появился 1 шт.) → 🏠 База → 🏗 Строить → «🌱 Теплица» (любое из перечисленных выше зданий подойдёт) → открыть превью. Правило решения: если превью показывает Верстак 1 как «в наличии» (не в списке нехватки) → `устранено`, жалоба была про путаницу игрока, а не про код; если превью пишет «не хватает: Верстак 1» при факте владения ≥1 шт. → `не устранено`, дефект в `checkCraftedItems`/`crafted_items_log`.
**Черновик ответа:** —
**Сомнения:** без прогона на реальном персонаже вывод «в актуальном коде дефекта нет» остаётся гипотезой; найденный мёртвый чек в легаси-броне — отдельный латентный баг (слишком мягкий гейт, кто угодно может крафтить броню без Верстака), к этой жалобе не относится, но стоит на заметку.

### `4294974508` — 2026-08-12 — Max Syskov

**Суть:** `[MEDIA]` «При нажатии на навесL1» — тап по уже построенному «⛺ Навес L1» на территории базы отвечал «Неизвестное строение, в классе: BuildingHandlerAction» (видно на скриншоте).
**Вердикт:** устранено
**Чем доказано:** скриншот `4294974508.jpg` — сообщение бота дословно «Неизвестное строение, в классе: BuildingHandlerAction» сразу после списка построек с «Навес L1» среди них. Коммит `c03cf7a8` (12.08.2026) завёл `case 'LeanTo':` в switch `BuildingHandlerAction::handle()` — код прочитан, case на месте, комментарий в коде прямо описывает этот же баг («тап по ⛺ Навес L1 отвечал Неизвестное строение»). Регрессионный гейт зелёный: `vendor/bin/phpunit --no-coverage --no-progress tests/unit/Camp/BuildingHandlerCoverageTest.php` → `OK, Tests: 3, Assertions: 6` — тест сканирует исходник хендлера на метки `case` и гарантирует ровно то, что умеет проверить: ни одна постройка из `Config\Buildings`, включая LeanTo, не останется БЕЗ `case`. Ветку, которая есть, но ничего не рисует (сломанный конкретный handler за уже заведённым `case`), скан исходника не поймает — это класс «забыли завести case», а не класс «case есть, а экран сломан» (урок `feedback_source_scan_tests_are_not_coverage`).
**На проде:** `git tag --contains c03cf7a8a1f0c75f23aba964269d7c46dbbf1a58 | sort -V | head -1` → `v0.51.607`, что не новее `v0.51.666` — правка в прод-релизе.
**Живой прогон:** не нужен
**Черновик ответа:** Починили: *⛺ Навес* теперь открывается нормально. Зайди на свою базу и тапни по нему ещё раз — экран здания появится.
**Сомнения:** сам коммит-фикс называет репортёром «Анжела», а в ветке это сообщение 12.08 прислал Max Syskov — расхождение в имени, но текст ошибки на скриншоте дословно совпадает с описанием бага в коммите, так что привязка к дефекту не вызывает сомнений (возможно, было два независимых репорта одного бага, второй — вне этой ветки).

### `4294974616` — 2026-09-10 — Main

**Суть:** `[MEDIA]` «На второй базе не могу построить навес» — в списке построек на второй базе кнопки «⛺ Навес» нет (скриншот показывает остаток недостроенного: Арсенал/Вышка связи/Деревянная стена/Колючая ограда/Дозорная вышка, Навеса среди опций тоже нет).
**Вердикт:** не баг
**Чем доказано:** это ДРУГОЙ дефект, чем `4294974508` (там — забытый `case` в switch, уже пофикшен; здесь — намеренное поведение, а не поломка). Прочитаны целиком `BuildListAction::handle()` и `FirstShelterService`: кнопка «Навес» добавляется в список построек ТОЛЬКО через `FirstShelterService::shouldOffer()`, а её докблок и код прямо описывают one-shot ПО ПЕРСОНАЖУ, не по базе — `hasAnyBuildingOrInflight()` считает `character_buildings` по `character_id` без фильтра по `map_cell_id`; как только у персонажа есть хоть одна постройка НА ЛЮБОЙ базе, Навес больше не предлагается нигде (комментарий в сервисе: «после первой стройки кнопка исчезает = one-shot», ADR-142). Прод-SELECT: `game_settings.onboarding.first_build.enabled = 1` — фича живая на проде, а не dormant, то есть игрок видит именно спроектированное поведение.
**На проде:** SELECT `game_settings` на проде подтверждает killswitch включённым; код с этим поведением в том же прод-релизе, что и `v0.51.666`.
**Живой прогон:** не нужен
**Черновик ответа:** —
**Сомнения:** нет по коду и по данным — «Навес» спроектирован как разовый подарок новичку, а не повторяемая постройка на каждую базу; вопрос может быть к тому, что игрок об этом не знает (кандидат на `/guide`/`tips`-ревью, не на фикс).

### `4294974671` — 2026-09-12 — Анжела

**Суть:** «На карте не отображается вторая база»; приложенный скриншот `4294974672` показывает меню «База» и «Декор базы», а не сам вид карты — доказательство добыто чтением кода и прод-SELECT, а не картинкой.
**Вердикт:** не устранено
**Чем доказано:** `TextMapService` прочитан целиком: «своя база» на карте определяется ОДНОЙ строкой — `claimedCellModel->where('character_id', ...)->where('status', 'active')->first()` без `orderBy` — и только эта пара координат помечается как «своя» (сравнение `$worldX === $baseX && $worldY === $baseY`); любая ДРУГАЯ активная база того же персонажа не попадает ни в «свои», ни в «чужие» (чужие явно отфильтрованы условием `character_id !== ...`) — то есть рисуется как обычная неизвестная клетка. Персонаж восстановлен через связанную жалобу `4294974674` того же автора в тот же день: скриншот `4294974675` называет координаты x=39,y=832 → `map.id=39833` → `claimed_cells.character_id=1107` (SELECT). Повторён точный запрос сервиса на проде: `SELECT id, map_cell_id, status FROM claimed_cells WHERE character_id=1107 AND status='active' LIMIT 1` → вернул `map_cell_id=767753` — это ПЕРВАЯ база Анжелы; вторая (39833) в принципе не может отметиться на карте её же персонажа.
**На проде:** прод-SELECT воспроизводит именно продовый запрос сервиса; код `TextMapService` идентичен HEAD ветки, без незакоммиченных правок.
**Живой прогон:** не нужен
**Черновик ответа:** —
**Сомнения:** сама жалоба `4294974671` не называет координат; привязка к character_id=1107 сделана через совпадение автора и даты со смежной жалобой `4294974674`, а не через прямую ссылку в тексте — обе, тем не менее, про одну и ту же вторую базу той же Анжелы в одну и ту же сессию.

### `4294974674` — 2026-09-12 — Анжела

**Суть:** «На второй базе постройки первого уровня при попытке поднять уровень пишут, что уровень уже максимальный, десятый»; скриншот `4294974675` показывает базу x=39,y=832 (Тропические джунгли) с постройками L1 (Теплица/Ручная скважина/Спортзал/Склад/Деревянная стена) и текст бота «Здание уже достигло максимального уровня (10)».
**Вердикт:** не устранено
**Чем доказано:** `BuildingUpgradeValidator::validate()` ищет постройку персонажа по ТИПУ здания — `character_id`+`building_id`, где `building_id` берётся как общий шаблон из `buildings.id` (одинаковый для всех баз игрока: `GreenhouseHandler::handle()` формирует кнопку через `$buildingModel->idByNameEn('Greenhouse')`, без учёта текущей базы) — без фильтра по `map_cell_id` и без `orderBy`, `.first()`. Прод-SELECT для character_id=1107: `character_buildings` держит ОБЕ Теплицы игрока (`id=343, map_cell_id=767753, level=10` — первая база; `id=453, map_cell_id=39833, level=1` — вторая, координаты из скриншота совпадают). Повторён точный запрос валидатора: `SELECT id, map_cell_id, level FROM character_buildings WHERE character_id=1107 AND building_id=5 LIMIT 1` → вернул `id=343, level=10` — то есть при попытке апгрейда L1-Теплицы со второй базы игра сверяется с L10-Теплицей первой базы и отвечает «уже максимум (10)». Совпадает дословно с текстом жалобы и скриншота.
**На проде:** прод-SELECT воспроизводит баг напрямую на данных реального персонажа; код валидатора идентичен HEAD ветки, правки нет ни в develop, ни в vulyk/*.
**Живой прогон:** не нужен
**Черновик ответа:** —
**Сомнения:** нет — прямое воспроизведение на живых прод-данных того же игрока, что и в скриншоте.
