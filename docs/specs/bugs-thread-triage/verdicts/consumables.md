<!-- mids: 4294974461 4294974495 4294974617 -->

# Вердикты: расходники, медицина, еда

### `4294974461` — 2026-07-13 — Alex

**Суть:** если сначала применить Успокоительное, а следом Стимулятор, второй препарат неверно берёт начальные здоровье/выносливость, не учитывая уже применённое Успокоительное — эффекты гасят друг друга вместо сложения. Скриншот `4294974462`: после Успокоительного «Выносливость: 35 → 65», но у следующего сразу Стимулятора «до» показано как 5, а не 65 — учтено не то состояние.
**Вердикт:** устранено
**Чем доказано:** докблок `CharacterStatsService` дословно цитирует этот багрепорт («Класс бага (багрепорт 2026-07-13, «Успокоительное → Стимулятор»)») — коммиты `6bedae88`/`47e2044d` того же дня заменили запись «снапшот в начале запроса → абсолютные значения» на короткую транзакцию с `SELECT ... FOR UPDATE`: дельта применяется к СВЕЖИМ статам под row-lock'ом. `UsePharmacyAction::applyMedicineEffect()` зовёт именно этот сервис. Проверил не наличие сервиса, а исход: синтетический вебхук-прогон на preprod-testbot (char 491, tg 6995661239) — baseline `health=49.90, tired=93.30`; **параллельно** (два `curl` в фоне, не последовательно) отправлены `usePharmacy_Sedative` (+5/+30) и `usePharmacy_Stimulator` (+25/+15), оба HTTP 200, `player_action_log` — id 668/669, `action_name='usePharmacy'`, `status='ok'` у обоих. Итог в БД: `health=79.90` (49.90+30, оба +5 и +25 сложились без потерь), `tired=100.00` (капнуто от суммы 138.30, а не от одного препарата) — обе дельты применились, ни одна не потеряна гонкой. Состояние char 491 и списанные заряды восстановлены после прогона.
**На проде:** `git tag --contains 47e2044d | sort -V | head -1` → `v0.51.550` ≤ `v0.51.666` — правка в прод-релизе.
**Живой прогон:** не нужен для того, что прогон реально проверил — сохранённое в БД состояние (`health`/`tired`) после пары применений, а не отрендеренный текст. Симптом со скриншота `4294974462` — это напечатанное в сообщении второго препарата значение «до» (было «5» вместо «65»); отрендеренную карточку второго применения синтетический прогон не смотрел, он читал только итоговые статы в БД. Tier-3 в реальном Telegram (MCP Chrome) — зона story 07, и именно там стоит доверить финальную проверку экрана.
**Черновик ответа:** Починили: если применить Успокоительное и следом Стимулятор, второй препарат теперь учитывает уже применённый эффект первого — они складываются, а не затирают друг друга.
**Сомнения:** нет

### `4294974495` — 2026-08-09 — Kinder surprise

**Суть:** «в инвентаре появился бесконечный медовый сбитень. Пишет что в остатке 1, но по факту не заканчивается». Владелец ответил `4294974496` «беру в работу».
**Вердикт:** устранено
**Чем доказано:** коммит `c122a9c9` (тот же день, 15:11, ~4 часа после жалобы) прямо цитирует багрепорт и называет причину: `decrementItemUsage` читал `durability_count` как счётчик доз КАК ЕСТЬ, а PvE-награда писала туда захардкоженную 100 при базе шаблона 1 (у мёда — предмет одноразовый) — 71 бесплатное применение при видимом «Остаток: 1 шт.». Правка — `CraftedItemsLogModel::baseCharges()`/`effectiveCharges()` = `max(1, min(остаток, база))`, зажим на ТОЧКЕ ЧТЕНИЯ; в тот же коммит нормализованы 19 строк прода. **Дефект в коде:** устранён — `decrementItemUsage` сейчас читает заряды только через зажим (проверено чтением текущего `UsePharmacyAction.php`, метод не менялся с `c122a9c9`). **Артефакт в данных прода:** SELECT сейчас (не на момент фикса) — 0 строк во всём `crafted_items_log` с `type IN ('drug','food')`, где `durability_count > GREATEST(1, база_шаблона)`; конкретно предмет «Медовый сбитень» (`crafted_items.id=93`, `name_eng=WinterHoneyMead`, база=1) — все 3 живые строки инвентаря (чары 1280, 1145, 644) имеют `durability_count=1`, ровно как база; раздутых зарядов нет ни у кого. Синтетический вебхук-прогон выше (доказательство `4294974461`) заодно подтвердил зажим живьём: у Стимулятора/Успокоительного на testbot до применения лежал остаточный `durability_count=100` при базе шаблона 1 — после списания оба ушли в 1/1, не в 99.
**На проде:** `git tag --contains c122a9c9 | sort -V | head -1` → `v0.51.603` ≤ `v0.51.666`.
**Живой прогон:** не нужен — прод-SELECT по актуальным данным уже покрывает и код, и артефакт.
**Черновик ответа:** Починили: «Медовый сбитень» и другие расходники больше не копят скрытые бесплатные применения — счётчик доз всегда совпадает с тем, что написано на экране.
**Сомнения:** нет

### `4294974617` — 2026-09-11 — Main

**Суть:** «В крафтовых предметах есть еда, которая не отображается в аптечка-провизия». Скриншот `4294974617` — раздел «🍱 Еда» с «📦 Мясо дикого кабана | 2 шт.» (вероятно, экран инвентаря/крафтовых предметов).
**Вердикт:** не устранено
**Чем доказано:** SELECT на проде — `crafted_items.id=23` «Мясо дикого кабана» (`name_eng='Wild Boar Meat'`) имеет `type='food'`, а не `'drug'`. И `PharmacyAction.php`, и `ProvisionAction.php` (обе полки бывшей аптечки, `pharmacy-split`) фильтруют выборку `WHERE crafted_items.type = 'drug'` — предмет структурно не попадает НИ на «Аптечка», НИ на «Провизия» независимо от `Config\Consumables::MEDICINE/PROVISION` (те списки вообще не читаются, пока строка отсеяна на уровне SQL). Это не единичный сирота: тем же `type='food'` помечены ещё `id=24` «Рыбный суп» и `id=25` «Ягодный джем» — у всех трёх задан `character_boost` (heal/stamina), то есть это настоящие лечебные/сытные предметы, а не декоративный лут; ни в одном текущем файле `app/` нет ссылки на их `name_eng` (`Wild Boar Meat`/`Fish Soup`/`Berry Jam`) — единственный обработчик применения расходников (`usePharmacy_<name_eng>`) для них так же недостижим, как и обе полки. Показываю масштаб, а не гипотезу: SELECT по `crafted_items_log` на проде — id23 держат 5 игроков (13 шт. суммарно), id24 — 3 игрока (6 шт.), id25 — 6 игроков (9 шт.) — дефект живой прямо сейчас у реальных людей. Отдельно от id23-25 существуют похожие по названию, но другие предметы `id=107 AutumnBerryJam`/`id=125 FishSoup` с `type='drug'` — те корректно долетают до «Провизии»; путаницу создают именно старые дубли `id 23-25`, не они.
**На проде:** прод-SELECT выше сделан по актуальной проде (не по develop); `git log` по `PharmacyAction.php`/`ProvisionAction.php`/`Config/Consumables.php` не содержит несмёрженной правки под эту жалобу ни в `develop`, ни в `vulyk/*` — фильтр `type = 'drug'` не менялся вовсе.
**Живой прогон:** не нужен — дефект доказан структурно (буквальный `WHERE` в обоих контроллерах) и подтверждён данными прода; поведение экрана тут не зависит от окружения.
**Черновик ответа:** —
**Сомнения:** не проверено, было ли `type='food'` у этой тройки намеренным дизайн-решением на момент их создания (например, задумывались как редкий трофей без экрана применения) — но `character_boost` с heal/stamina и отсутствие любого другого пути потребления говорит скорее про забытых сирот, чем про замысел; это решение не моё — фиксирую находку, а не чиню.
