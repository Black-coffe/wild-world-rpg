<!-- mids: -->

# Живой прогон: исполненные скрипты тапов

ЗАГЛУШКА — заполняет `bugs-thread-triage-07`. Шапку `mids` воркер пишет сам: в неё попадают ровно те
`mid`, по которым он исполнил скрипт тапов или посмотрел скриншот. Формат блока — `_FORMAT.md`;
в поле «Чем доказано» — что именно наблюдалось (экран, текст кнопки, строка ответа бота, вывод SQL).
