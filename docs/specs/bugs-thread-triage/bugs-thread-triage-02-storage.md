---
story: bugs-thread-triage-02
spec: bugs-thread-triage
status: done
tier: 3
worker: worker-code
tracer: false
wave: 1
blocked_by: []
---

# Вердикты: хранилища и инвентарь

## Goal

По трём жалобам про склад, рюкзак и общий пул ресурсов вынесен вердикт из шести
(`verdicts/_FORMAT.md`) против актуального **прод**-состояния. Там, где без браузера не закрыть,
лежит исполнимый скрипт тапов по контракту C4 плана.

## Requirements

> проходишь каждое сообщение или набор сообщений, чтобы контекст полный был, и смотришь: мы эту систему поправили, баг устранили?

> Если да, пишешь ответ, что данная система устранена, бага нет.

## Files
- docs/specs/bugs-thread-triage/verdicts/storage.md

## Линия отсечения — прод, а не `develop`

Прод стоит на **`v0.51.666`** (деплой 11.09), в `develop` поверх него 60 невыкаченных коммитов,
`vulyk/craft-shelf-coverage` не смёржена даже туда — `RECON.md`. «Нашёл коммит» и «игрок это видит»
— **разные утверждения**. Принадлежность проду: `git tag --contains <sha> | sort -V | head -1`
(тег не новее `v0.51.666`; `sort -V` обязателен), либо прод-`SELECT`. Правка есть, но тега нет или
он новее → вердикт `исправлено, но не на проде` + где лежит. Прод — только чтение
(`wildworld-bot@…` = ПРОД, `wildworld-testbot@…` = preprod, не перепутать).

## Жалобы (адреса реплая — `mid` из `thread.md`)

| `mid` | дата | автор | о чём |
|---|---|---|---|
| `4294974511` | 18.08 | Анжела | «Находясь на базе переместить лут на склад можно только дроном». Владелец ответил `4294974515` «Исправляем»; уточнение `4294974516`: «забрать со склада — кнопкой, а выложить — только дроном» |
| `4294974531` | 19.08 | Анжела | `[MEDIA]` «Теплица не видит воду, выложенную из рюкзака на склад» |
| `4294974532` | 19.08 | Анжела | два пункта в одном сообщении: (1) «Крафт ингредиенты доступны только из рюкзака, даже когда я стою на складе»; (2) «кнопка „забрать со склада“ должна работать так же, как и кнопка „положить на склад“: „выбор ресурса“ или „забрать все“». Скриншот `4294974533`, тег `4294974534` |

**Скриншоты** (`DATA.md`): `<scratchpad>/shots/{4294974531,4294974533}.jpg`, где `<scratchpad>` =
`C:\Users\Andrei\AppData\Local\Temp\claude\C--laragon-www-mmorpg\a3678868-a680-4784-9892-4add57aa9871\scratchpad`.
`4294974531` пришёл превью 122×800 и может не читаться — тогда это открытое сомнение либо пункт
для story 07 (перечитать вложение в Telegram Web), но не догадка.

## Гипотезы (проверить, не поверить)

- Пункт (1) из `4294974532` ↔ ADR-171 «пул рюкзак+склад» и `CraftCardHelper::available()`
  (`memory/map/craft.md`): доступность сырья на карточке крафта считается по пулу. Если пул
  живой — это кандидат на «устранено», но доказательство = карточка крафта у персонажа, стоящего
  на складе, плюс тег, а не наличие класса.
- `4294974531` — та же тема с другой стороны: теплица (ферма) могла остаться на старом чтении
  «только рюкзак», даже если крафт переехал на пул. Ресурс-грамотность («5 хранилищ = общий пул»)
  — это обещание; проверять достижимость отдельно для фермы.
- `4294974511`/`4294974516` ↔ спека `storage-craft-insurance` (approved 19.08). Совпадение темы
  не означает, что закрыли именно асимметрию «положить/забрать»; и отдельно проверь, доехала ли
  правка до прод-тега.
- Пункт (2) — про форму экрана (выбор ресурса / забрать всё). Вердикт по нему выносится отдельно
  от пункта (1) внутри того же блока, явным подпунктом.

## Non-goals

- Не чинить: ни строки в `app/`, ни миграции, ни теста.
- **Не считать коммит в `develop` или ветку `vulyk/*` доказательством «устранено»** — для этого
  есть отдельный вердикт.
- Не заходить в MCP Chrome и Telegram Web — браузер держит story 07; нужен тап или нечитаемое
  вложение, пиши пункт по C4.
- Не выносить вердикт по карточке крафта как таковой (дубли кнопок, английские слова) — это
  `bugs-thread-triage-04`; здесь только видимость складских ресурсов.
- Не мерить и не называть в черновике цифры вместимости/веса/скорости — они дрейфуют.
- Не писать в прод; на testbot — со снимком «до» и восстановлением.

## Map slice
`memory/map/player.md` (инвентарь, вес) · `memory/map/craft.md` (пул рюкзак+склад, ADR-171) ·
`memory/map/bases.md` (склад как постройка) ·
`docs/specs/bugs-thread-triage/RECON.md` (линия отсечения, доступы) · `DATA.md` (артефакты)

## Acceptance criteria
- [ ] Все три `mid` закрыты блоком с семью полями; оба пункта `4294974532` разведены явно.
- [ ] У каждого блока поле «На проде» заполнено; у `устранено` там тег не новее `v0.51.666`
      (полученный через `sort -V`) либо прод-`SELECT`.
- [ ] Ни один `устранено` не опирается только на коммит/спеку/ADR — есть маркер исполнения.
- [ ] Для `4294974531` сказано, читает ли теплица тот же пул, что и крафт, и чем это показано.
- [ ] Каждый `требует живого прогона` несёт предусловие, путь и правило решения (C4).
- [ ] Черновики только у `устранено`, одной строкой, с указанием действия игрока; парные `*`.

## Verification
`bash docs/specs/bugs-thread-triage/verdict-lint.sh docs/specs/bugs-thread-triage/verdicts/storage.md`

## Implementation notes

Три `mid` закрыты в `verdicts/storage.md`: `4294974511` — устранено (коммит `95c1cca3`, тег
`v0.51.626`, плюс прод-`action_log` подтверждает реальное использование обеих кнопок);
`4294974531` — устранено (коммит `d6b595c6`, тег `v0.51.631`, `GreenhouseProductionHandler` читает
пул склад+рюкзак напрямую через `BaseStorageModel`, killswitch `storage.pool_enabled=1` на проде);
`4294974532` — разведён на два независимых пункта внутри одного блока: пункт (2), про форму кнопки
«забрать со склада», устранено (коммит `dd758cc0`, тег `v0.51.631`); пункт (1), про видимость
складских ресурсов в крафте, — не устранено полностью (обычный крафт — устранено, T2-карточки
роботов/маяков — нет), итоговый вердикт блока `не устранено` по более слабой части.

## Findings

Обычный крафт (T1 `WorkbenchGeneral` и большинство T2 `WorkbenchStandard` — броня, оружие) уже
читает общий пул рюкзак+склад через `ResourcePoolService` (`CraftCardHelper::available()`,
`GenericCraftActionStart::checkResources()`). Но пять экранов T2 остались на старом чтении
«только рюкзак» мимо общего пула — `grep -rl ResourcePoolService` по этим файлам пуст, они читают
доступность напрямую через `CharacterResourceModel::getResourceByNameAndCharacterId()`:

- `app/Controllers/Telegram/Commands/Actions/Craft/WorkbenchStandard/Robots/RobotExplorer2Action.php`
- `app/Controllers/Telegram/Commands/Actions/Craft/WorkbenchStandard/Robots/RobotGatherer2Action.php`
- `app/Controllers/Telegram/Commands/Actions/Craft/WorkbenchStandard/Robots/RobotT2PreviewAction.php` (общий предок `RobotScout2Action`/`RobotIndustrial2Action`)
- `app/Controllers/Telegram/Commands/Actions/Craft/WorkbenchStandard/TeleportBeacon/TeleportBackpack2Action.php`
- `app/Controllers/Telegram/Commands/Actions/Craft/WorkbenchStandard/TeleportBeacon/TeleportBeaconBasic2Action.php`

Ключевая находка: для этого хвоста **коммита-кандидата не существует нигде** — ни в `develop`, ни
в `vulyk/*`. Это отличается от «исправлено, но не выкачено на прод»: правки просто не написано.
Игрок, стоящий на складе с ресурсами под робота или маяк, сегодня видит ❌ ровно как до фикса
`dd758cc0`/`fc3ba6b1`/`efa1c504`/`b9365c7d`. Кандидат на отдельную узкую story — миграция этих
пяти карточек на `ResourcePoolService` по образцу `GenericCraftActionStart`.
