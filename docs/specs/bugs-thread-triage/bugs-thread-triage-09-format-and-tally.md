---
story: bugs-thread-triage-09
spec: bugs-thread-triage
status: done
tier: 2
worker: worker-code
tracer: false
wave: 4
blocked_by: [bugs-thread-triage-08]
---

# Ремонт: седьмой вердикт, воспроизводимый учёт, верная цифра отсечения

## Goal

Три находки ревью, которые живут в служебных файлах спеки и блокируют доверие к остальному:
словарь вердиктов не умеет назвать «жалоба верна, но это не дефект кода»; закрывающий учёт
сообщений не воспроизводится из собственных списков; цифра невыкаченных коммитов завышена в полтора раза.

## Requirements

> Если да, пишешь ответ, что данная система устранена, бага нет.

> смотришь: мы эту систему поправили, баг устранили?

## Files
- docs/specs/bugs-thread-triage/verdicts/_FORMAT.md
- docs/specs/bugs-thread-triage/verdict-lint.sh
- docs/specs/bugs-thread-triage/plan.md
- docs/specs/bugs-thread-triage/RECON.md

## Что делать

1. **Седьмой вердикт** (находка MAJOR-5). Сейчас шесть значений, и ни одно не означает
   «жалоба игрока подтверждена фактами, но это не дефект кода — это дизайн или баланс».
   Из-за этого `4294974471` («зачем больше одного дрона») носит ярлык `не баг`, хотя тот же
   блок доказывает: находка реальна, двое живых игроков на проде в этом состоянии.
   Добавь значение **`подтверждено, но не дефект кода`**. Последствие у него то же, что у
   остальных не-`устранено`: **ответа в чат не уходит** (решение владельца на брифинге).
   Меняется только честность ярлыка, не поведение.
2. **`verdict-lint.sh`** — принять седьмое значение; черновик ответа у него по-прежнему обязан
   быть `—`. Прогони скрипт на всех семи файлах вердиктов, убедись, что ничего не сломал.
3. **Учёт (находка MAJOR-6).** Таблица «27 + 45 + 9 = 81» в `plan.md` не воспроизводится:
   в ней названы 48 id, из них два (`4294971813`, `4294974591`) вообще не сообщения ветки,
   пять посчитаны дважды (они же — mid'ы вердиктов), а 13 настоящих сообщений не названы нигде
   в этом разделе (одиннадцать из них названы внутри блоков вердиктов, два — в story).
   Жалоб при этом не потеряно ни одной — проверено ревью. Перепиши раздел так, чтобы **арифметика
   сходилась из тех id, которые он сам перечисляет**, а остаток был назван явно: «учтены внутри
   блоков вердиктов, вот где». Утверждение о полноте обязано быть доказательством, а не заявлением.
4. **Цифра отсечения (находка MINOR-7).** `RECON.md` говорит «60 коммитов мимо прода».
   Их **40**: `git rev-list v0.51.666..develop --count`. Ошибка возникла из-за `git log --since`,
   который считает по дате коммита и захватывает уже вошедшие в тег. Исправь цифру и **назови
   правильную команду** вместо прежней, чтобы следующий раз мерили по предку, а не по времени.
   Сама линия отсечения верна и подтверждена независимо — правится только число и метод.

## Non-goals
- Не трогать файлы вердиктов (кроме `_FORMAT.md`) и `REPORT.md` — у них свои story.
- Не менять поведение: седьмой вердикт не открывает новых сообщений в чат.
- Не чинить код игры.

## Map slice
`docs/specs/bugs-thread-triage/RECON.md`

## Acceptance criteria
- [x] `_FORMAT.md` несёт семь значений, и у седьмого явно записано «ответа в чат не уходит».
- [x] `verdict-lint.sh` принимает седьмое значение и зелен на всех семи файлах вердиктов.
- [x] Учёт в `plan.md` воспроизводится из собственных списков; остаток назван поимённо.
- [x] `RECON.md` говорит 40 и называет `git rev-list v0.51.666..develop --count`.

## Verification
`bash docs/specs/bugs-thread-triage/verdict-lint.sh docs/specs/bugs-thread-triage/verdicts/_FORMAT.md && grep -q 'rev-list' docs/specs/bugs-thread-triage/RECON.md && bash scripts/trace-check.sh docs/specs/bugs-thread-triage`

## Implementation notes

- `verdicts/_FORMAT.md`: добавлено седьмое значение `подтверждено, но не дефект кода` (после
  `не баг`) в оба места, где перечислялся набор («Вердикт — ровно одно из семи» + правило в списке
  проверок verdict-lint), с абзацем про семантику и «ответ не уходит». Также поджата шапка
  illustration-примера `<!-- mids: 4294974283 4294974469 4294974508 -->` → `<!-- mids: 4294974469 -->`
  — она заявляла три mid, а под ней шёл ровно один example-блок; несоответствие валило
  `verdict-lint.sh` на самом `_FORMAT.md` (проверено на `git show HEAD:...` — уже падало до моих правок),
  а story `## Verification` явно гоняет линт по этому файлу.
- `verdict-lint.sh`: `case`-проверка и текст ошибки набора вердиктов расширены до семи значений.
  Прогнан на всех семи файлах `verdicts/*.md` (bases/consumables/craft/live-run/robots/storage/world)
  — везде `exit=0`, ни один существующий блок не задет (седьмой вердикт никто ещё не проставил —
  это работа других story, заблокированных на этой).
- `plan.md`: пересобран раздел «Сообщения ветки, которым план НЕ назначил вердикт» — учёт построен
  программно (`comm`/`grep` по `thread.md` и `verdicts/*.md`), а не заявлен. Итог: 27 (свой блок
  вердикта) + 40 (контекст, назван в разделе) + 11 (назван прозой внутри блоков вердиктов, не в
  разделе) + 3 (назван только в story-файлах волны 1) = 81. Попутно найдено и исправлено: два id
  (`4294971813`, `4294974591`) — не сообщения ветки; пять id (`4294974528`, `4294974537`,
  `4294974543`, `4294974578`, `4294974601`) в старом разделе дублировали mid, уже закрытый своим
  блоком вердикта. Также поправлены две сопутствующие числовые ссылки на «шесть вердиктов»
  (C1-контракт и Goal) на «семь» и на «60 коммитов» → «40» с командой (это то же MINOR-7, просто
  число повторено в живом тексте плана, а не только в RECON.md) — историческую запись про 60 в
  `## Plan deltas` от 2026-09-12 не трогал, это append-only журнал прошлого раунда.
- `RECON.md`: цифра 60 → 40, метод исправлен на `git rev-list v0.51.666..develop --count`
  (проверено командой); названа причина ошибки прежнего `git log --since=`.
- Полная цепочка `## Verification` прогнана целиком — зелёная (см. `## Findings`).

## Findings

Стены нет — все три находки (седьмой вердикт, учёт, цифра отсечения) закрыты первым проходом.
Единственная неожиданность: сам `## Verification` из story требовал прогонять `verdict-lint.sh`
по `_FORMAT.md` — файлу-шаблону, а не боевому вердикт-файлу, и это падало ещё до моих правок из-за
рассинхрона иллюстративной шапки с примером. Поправил шапку примера (см. заметки выше), а не стал
обходить это как «известный шум гейта», раз фикс укладывался в файл, который и так в `## Files`.
