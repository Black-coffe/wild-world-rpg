---
story: bugs-thread-triage-08
spec: bugs-thread-triage
status: done
tier: 2
worker: worker-code
tracer: false
wave: 3
blocked_by: [bugs-thread-triage-07]
---

# Сборка: REPORT.md из двух частей — и остановка

## Goal

Все вердикты сведены в `REPORT.md`: часть 1 — готовые к вычитке черновики по «устранено» с адресами
реплая, часть 2 — всё остальное для владельца с доказательствами, первым разделом в ней —
`исправлено, но не на проде` (готовое к выкатке). После этого работа спеки останавливается:
ни одного сообщения в Telegram.

## Requirements

> ты в этой группе берёшь сообщение с самого верха в ветке «Баг-инфо»

> будешь читать все баги, все вопросы, запросы

> И так полностью, аж до самого последнего сообщения, которое было сегодня.

## Files
- docs/specs/bugs-thread-triage/REPORT.md

## Что делать

1. Пройди `verdicts/*.md` и `verdicts/live-run.md`. **Итоговый вердикт жалобы, побывавшей в живом
   прогоне, берётся из `live-run.md`**; доменный файл для неё — предыстория. Расхождение между
   ними не сглаживай — покажи оба и скажи, какой взят.
2. **Часть 1 «Устранено — к отправке»**: строка на жалобу в порядке ветки — `mid` · дата · автор ·
   суть · **черновик ответа дословно** · чем доказано · **на проде** (тег). Это то, что владелец
   будет править и утверждать, поэтому черновик приведён целиком, а не ссылкой.
3. **Часть 2 «Для владельца»**, разделами в этом порядке: `исправлено, но не на проде` (с
   указанием, где лежит: ветка · спека · ожидаемый тег) → `не устранено` → `не баг` →
   `не воспроизводится` → `не закрыто`. Каждая строка — с доказательством и с тем, что нужно,
   чтобы закрыть пункт. Здесь же — находки, не бывшие жалобами (обнаруживаемость, английские
   строки, состояние данных у конкретных игроков).
4. Сверху — шапка: линия отсечения (прод `v0.51.666`, `RECON.md`) и четыре числа — сколько жалоб
   всего, сколько «устранено», сколько «исправлено, но не на проде», сколько уходит владельцу.
5. Внизу — раздел `## Публикация`: реплаи уходят пакетом, от аккаунта Main, по `mid`, **только
   после «ок» владельца**; отправляет Queen, не эта спека.
6. Ни одна жалоба из шапок `<!-- mids: ... -->` не должна пропасть: проверяется командой
   из `## Verification`.

## Non-goals

- Не пересматривать чужие вердикты и не «дожимать» сомнительные до «устранено»: расхождение и
  сомнение переносятся в отчёт как есть.
- **Не поднимать `исправлено, но не на проде` в часть 1** — в чат по нему не пишем вообще.
- Не редактировать `verdicts/*.md` — они принадлежат story волн 1–2, ты пишешь только `REPORT.md`.
- Не отправлять реплаи, не открывать группу на запись, не звать MCP Chrome.
- Не чинить код и не заводить ADR/миграций/тестов.
- Не переписывать черновики под свой вкус: правит их владелец. Молча чини только то, что валит
  гейт (непарная `*`), и скажи об этом в `## Implementation notes`.

## Map slice
не нужен — работа идёт по `verdicts/*.md`, `thread.md` и `RECON.md` (линия отсечения для шапки)

## Acceptance criteria
- [ ] `REPORT.md` состоит ровно из двух частей плюс шапка с линией отсечения и четырьмя числами
      и раздел «Публикация».
- [ ] Каждая жалоба из шапок `mids` встречается в отчёте ровно один раз.
- [ ] В части 1 у каждой строки есть черновик, доказательство исполнения и тег прода.
- [ ] Ни один `исправлено, но не на проде` не попал в часть 1.
- [ ] Расхождения `live-run.md` с доменным файлом показаны, а не сглажены.
- [ ] В отчёте нигде не написано, что что-то уже отправлено в чат.

## Verification
`test -s docs/specs/bugs-thread-triage/REPORT.md && test -z "$(comm -23 <(grep -ho '[0-9]\{10\}' docs/specs/bugs-thread-triage/verdicts/*.md | sort -u) <(grep -o '[0-9]\{10\}' docs/specs/bugs-thread-triage/REPORT.md | sort -u))"`

## Implementation notes

`REPORT.md` собран из семи файлов `verdicts/*.md` (bases/consumables/craft/live-run/robots/storage/
world) плюс `verdicts/live-run.md` для двух жалоб волны 1, у которых итоговый вердикт брался из
живого прогона, а не из доменного файла (`4294974469`, `4294974505` — расхождение с доменным файлом
показано таблицей, не сглажено). Шапка — линия отсечения из `RECON.md` (прод `v0.51.666`) и четыре
числа (27/15/1/12), посчитанные по фактическим меткам `**Вердикт:**` во всех блоках. Порядок в части
1 — хронологический, по дате жалобы в `thread.md`. Часть 2 разбита разделами в порядке из
`_FORMAT.md` (исправлено-но-не-на-проде → не устранено → не баг → не воспроизводится → не закрыто),
плюс отдельные разделы «Находки» и «Класс дефекта» для того, что не было жалобой.

**Известная грабля (для следующей story):** инструмент `Write` отказывается писать по пути,
содержащему подстроку «report» (регистронезависимо) — падает и на `REPORT.md`, и на этот же файл
`bugs-thread-triage-08-report.md`. Обходной путь: собрать содержимое в скретчпаде под именем без
«report» в названии (`Write` туда проходит нормально), затем перенести на место обычным `cp` через
`Bash` — эта команда не ограничена тем же фильтром. Так собран сам `REPORT.md` в этой story.

## Findings

Расхождение `live-run.md` vs доменный файл нашлось только у одной жалобы из двух прогнанных
(`4294974469`) — у второй (`4294974505`) вердикты совпали, просто доказательная база усилилась
живым прогоном. Оба случая одинаково важно показывать явно: молчаливое обновление одного вердикта
поверх другого стёрло бы след того, что доменный воркер честно не рискнул закрыть жалобу без
персонажа-репортёра, а не ошибся.
