---
spec: community-chat-bot
tier: 4
status: repair-round (два ревьюера дали BLOCK; волна 8 — ремонт)
approved: 2026-08-25 (владелец: три открытых решения делегированы Queen, зафиксированы в §16)
date: 2026-08-25
review: панель из 5 (архитектор, лор-хранитель, гейм-дизайнер, игрок, модератор) — вердикты сведены
---

# План — Роби в общем чате игроков

## 1. Что просили

Добавить существующего игрового бота в общий чат игроков; бот собирает всё, что там пишут,
складывает на прод, а при запуске Claude Code данные синхронизируются на локальную машину.
Бот отвечает игрокам от своего лица, чтобы группа была живой, когда владелец не успевает.
Отдельно спланировать: режим ответа (авто / полуавто / ручной) и защиту от того, чтобы
через бота выуживали фишки, лайфхаки и читерские пороги.

Ответы владельца: чат — **супергруппа с темами**; скорость — **двухскоростная**;
подпись — **бот своим лицом**.

> ⚠️ Персонажа зовут **Роби**, одна «б» (`GAME_DESCRIPTION.md:367`, `TipService.php:129`,
> `ColdOpenGreetingService.php:42`). В брифе он записан со слов владельца как «Робби» —
> бриф не правим, но во всех текстах и в коде только «Роби».

## 2. Что показала разведка (факты, не предположения)

| Факт | Где | Следствие для плана |
|---|---|---|
| Проверки `chat.type` в вебхуке нет вообще | `BotController.php:45-153` | Бот в группе начнёт отвечать «Не понял команду» на каждую реплику. Гейт — предусловие №1 |
| Rate-limit считает окно по `from.id`, лимит 60/мин | `TelegramRateLimitFilter.php:64,240-258` | Болтун в группе выест свой же **игровой** лимит и получит блокировку в игре |
| `setWebhook` / `allowed_updates` нигде в коде, только curl в докблоке | `BotController.php:41-43` | Что разрешено на стороне Telegram — из репо не видно, надо зафиксировать версионно |
| Вся исходящая инфраструктура шлёт в личные `chat_id` игрока | `BroadcastService.php:41-66` | Отправки в группу+топик не существует, нужен отправитель с `message_thread_id` |
| Таблицы входящих сообщений нет | миграции | Нужны новые таблицы, значит обязательна классификация в `WipeManifest` |
| rsync исключает `.claude memory scripts` | `.github/workflows/deploy.yml` | Скриптов на проде нет. Канал синка — HTTP с секретом либо SSH, но не «скрипт на сервере» |
| `app/Commands/` из rsync НЕ исключён (исключён только `scripts/`) | `.github/workflows/deploy.yml` | Удалённое исполнение spark-команд на проде уже есть — новую HTTP-поверхность открывать не нужно (ADR-176) |
| `GuideCatalog::sections()` рендерится ПО ЖИВЫМ флагам | `GuideCatalog.php:59-63,71-76,84-89` | Скопированный в банк текст замёрзнет и начнёт врать про кнопки. Хранить адрес раздела, не тело |
| `canon_reviewed=1` — отметка о разовой сверке, не свежесть | `ApplyCanonCorrections.php:21,80` | Флаг не сбрасывается после ребаланса. Нужен доп. критерий по дате |
| `GAME_DESCRIPTION.md` синхронизирован по состоянию на `v0.51.148` при проде ~`v0.51.649`, внутри — таблица открытых расхождений | `GAME_DESCRIPTION.md:743-751` | В корпус ответов не входит. Источник для человека, не для банка |
| ~30 живых килсвитчей: `pvp.duel.enabled`, `world.vehicle.enabled`, `oracle.enabled`, `craft.shortfall_buy.enabled`, `faction.project.enabled`… | grep по `app/` | Замороженный банк переживёт выключение фичи и продолжит про неё рассказывать |
| Опасный корпус: `GameBalance.php` 396 строк, значения `game_settings`, 30 файлов с `rarity chance probability` | `app/Config/GameBalance.php` | Это и есть «фишки и читинг» из заказа |
| В `GameBalance.php` уже зафиксирован рассинхрон: `luckyStrikeMaxChance = 40  // ⚠️ 30 в GAME_DESCRIPTION.md` | там же | Бот, назвавший число, соврёт даже без злого умысла |

## 3. Архитектура: две скорости, один банк

```
              ПРОД (всегда)
  Игроки в чате -> webhook -> гейт по типу чата -> CommunityIngest
                                                    |
                                            community_messages
                                                    |
                            матчер <- банк ответов (утверждён владельцем)
                              |                        |
                        совпал |                        | не совпал
                              v                        v
                    гвард (5 рубежей)            реакция 👀 +
                              |                  «открытый вопрос»
                              v
                    CommunityChatSender -> ответ в топик

       ssh spark community:export  ^  ssh spark community:import
                    |                 |
                    v                 |
              ЛОКАЛЬНО (когда запущен Claude Code)
  SessionStart-хук тянет inbox -> .claude/community/
  скилл /community: черновики + редколлегия
     (лор-хранитель · гейм-дизайнер · анти-спойлер · редактор)
  -> пуш черновиков на прод
                    |
                    v
  /admin/community — очередь: Одобрить / Правка / Отклонить / Отозвать
     одобрено -> отправлено реплаем + пополнило банк
```

**Почему так, а не иначе.** Заказ содержит противоречие: «работает только когда я запускаю
Claude Code» и «чтобы группа была живой». Живая группа — это ответ в минутах, а сессия
случается раз в день-два. Банк ответов разводит эти два требования: генерация всегда
происходит при запущенном Claude Code и всегда проходит через владельца, а **отдача**
заранее утверждённого текста работает круглосуточно и не требует ни ИИ на сервере, ни
API-ключа, ни расходов на каждый ответ. Ни одного слова, которого владелец не видел,
бот в чат не скажет.

## 4. Режим ответа — четыре полосы, не две

Панель развернула исходную матрицу: она была написана со стороны сервера, а не со стороны
чата. Главная дыра — не было **адресного обращения**, ради которого игрок ботом и пользуется.

| Полоса | Что это | Режим | Задержка |
|---|---|---|---|
| **A. Адресное** | `@бот`, реплай на сообщение бота, «Роби, …» | Отвечает **всегда**. Совпал банк — текст; не совпал — утверждённое «Не знаю точно. Передал вопрос разработчику» | 0 сек — перебивать здесь некого |
| **B. Подслушанное** | Вопрос в топике, не адресованный боту | Авто только при **высоком** пороге матча | Выдержка 60–90 сек; отменяется, если ответил человек |
| **C. Стоп-тема / приватное** | §6 и §7 | Бот не отвечает по существу. Реакция 🤔 + эскалация владельцу | — |
| **D. Не вопрос** | Флуд, мемы, разговор, обращение к другому человеку («Вась, а как…») | Молчит полностью, без реакции | — |

**Признанный компромисс (был скрыт в первой редакции).** Тормоз «не отвечать, если ответил
человек» реализуем только как задержка отправки — значит «мгновенный слой» мгновенен **только
в полосе A**. В полосе B он намеренно медленный: перебивание живых людей — главная причина,
по которой из чатов с ботами уходят.

**Детектор вопроса — не по знаку «?».** В русском чате «а как ваще крафтить» — вопрос,
«серьёзно?» — нет. Правило: вопросительные слова + минимальная длина + чёрный список
междометий. Порог матчера для полосы B заметно выше, чем для A.

**Реакции вместо молчания.** `setMessageReaction` — самый дешёвый способ не выглядеть
мёртвым: 👀 «вопрос принят в очередь», 🤔 «стоп-тема», флуд — ничего. Реакция не занимает
строку и никого не перебивает. Полное молчание читается как «бот сломался», и через неделю
бота перестают спрашивать вовсе.

**Отложенный ответ обязан быть оформлен**, иначе это некро-постинг: реплай на исходное
сообщение, обращение к автору, признание задержки («@nick, возвращаюсь к твоему вопросу про
маяки»). Вопрос старше 48 часов публично не отвечается. Пять одинаковых вопросов склеиваются
в один ответ, а не в пять сообщений подряд.

**Тормоза — все ключи `GameSettings`:** потолок ответов в час на топик; кулдаун **на автора**,
а не только на топик; лимит длины авто-ответа; список топиков, где бот молчит всегда; выдержка
полосы B. При срабатывании потолка бот молчит **полностью**, а не через раз — выборочное
молчание читается как «мне не отвечают».

## 5. Защита от выуживания читов — пять рубежей

Первая редакция имела три и все три обходились. Панель нашла обходы, рубежей стало пять.

**1. Белый список корпуса — с двумя поправками.**
Черновики собираются только из `GuideCatalog`, `game_tips`, `glossary`, опубликованных
`site_posts`. `GameBalance.php`, `game_settings` и файлы с `rarity|chance|probability` в
список не входят — не «не читать их», а не иметь их в источниках. Поправки лор-хранителя:
- `/guide` цитируется **адресом раздела, а не телом**: `sections()` собирает текст по живым
  флагам, копия замёрзнет и начнёт врать про кнопки.
- для `site_posts` мало `canon_reviewed=1` — флаг не сбрасывается после ребаланса; нужен
  ручной список слагов либо ограничение по дате последней сверки.
- `GAME_DESCRIPTION.md` исключён и остаётся исключённым: синхронизация заявлена по
  `v0.51.148` при проде `v0.51.649`, внутри — собственная таблица открытых расхождений и
  плотные числа баланса.

**2. Провенанс — вспомогательная пометка, БЕЗ права вето; главный рубеж — форма и лексика
(ADR-177, ADR-178).**
🔴 Этот пункт переписан story 63/65/69/70/72/73 (волна 9) — версия выше («провенанс — главный
рубеж, нет источника — нет авто-режима») не описывает механику, которая существует в коде, и её
нельзя больше цитировать как основание. Найдено (§17, находка 1) и **закрыто**: измерено, что
лексическое покрытие детектирует ДОСЛОВНОСТЬ, а не ПРАВДИВОСТЬ — законный пересказ своими
словами и лучший несравнительный фабрикат перекрывались по значению, порога между «правда
проходит, выдумка нет» не существует. Держать провенанс вето значило резать бо́льшую часть
живых ответов владельца. Полное обоснование — ADR-177 (единица подтверждения) и ADR-178
(потеря права вето, четыре поправки).

🔴 **Единственная полная гарантия — человек.** Авто-режим отдаёт исключительно текст со
`status='approved'` — каждое слово одобрено владельцем, факт одобрения уходит в
`admin_audit_log`. Гвард — сито, снижающее нагрузку на этого человека и детерминированно
ловящее механические классы утечки, а не барьер, который его заменяет. Ревьюер, требующий от
гварда гарантии правдивости, требует несуществующего по устройству признака.

Что гвард **детерминированно запрещает**:
- числа, числительные словами (по границе слова) и вопросы с числом/диапазоном/«%»/порогом или
  формой проверки гипотезы («правда, что…», «…, да?») — рубеж 2 (входящее) и рубеж 3 (лексика);
- стоп-темы — по фразо-буквальным маркерам стоп-листа (рубеж 4);
- механику, выключенную килсвитчем — обязательное поле `requires_setting`, рубеж 5 (live vs
  dormant);
- **форму** совета через R1–R4, любое из четырёх правил достаточно для `deny`: сравнение и оценку
  (R1 союзы «чем»/«нежели»/«вместо»/«а не», R2 корень оценки без союза — «Лаборатория
  полезнее»), рекомендательный оборот и его зеркало (R3 «стоит делать X», R4 «попадаются чаще,
  если идти без брони»).

Что гвард **не держит и держать не может**:
- **правдивость утверждения** — законное описание механики без чисел и без
  сравнительно-оценочной формы («Узлы-боссы появляются только рядом с занятыми базами») ловит
  только одобряющий человек, не рубеж;
- **совет в повелительном наклонении** («Заходи в чужую базу, пока хозяин в походе») — R5
  (клауза-императив + маркер оптимизации по времени/моменту) существовал в story 73 и был
  отменён в story 78 (ADR-178, поправка №5): детектор опознавал «императив» по позиции
  подлежащего в клаузе, а начало клаузы в русском — прежде всего позиция существительного, не
  только глагола, и денил ядро того, что бот обязан говорить («Уровень здоровья восстанавливается,
  пока ты на базе»). Класс признан непокрываемым без словаря глагольных форм, который будет
  устаревать, и обходится тем же автором, против которого заведён, простым перефразом в
  описательную форму — цена ловли ложится на невиновного, цена обхода равна нулю для виновного;
- **совет в форме, не описанной R1–R4** — стоп-правило ADR-178 (поправка №4, подтверждено
  поправкой №5) запрещает добавлять новые правила формы без замеренного класса утечки на живом
  чате, значит непокрытая форма остаётся непокрытой не по недосмотру, а по решению;
- **перифраз стоп-темы** — стоп-лист фразо-буквален, естественная формулировка вопроса мимо
  списка слов проходит;
- **новизну машинных черновиков** — провенанс из рубежа снят (ADR-178) и остался пометкой
  качества для ревьюера в `community:import`, а не запретом на отправку.

Что есть сейчас (механика):
- **Единица подтверждения — предложение источника**, не документ и не окно поверх документа:
  корпус режется на предложения-юниты, mini-IDF считается по ним же, подтверждение набирается
  ОДНИМ юнитом целиком, никогда объединением. Скользящего окна анти-рекомбинации и его четырёх
  констант (`RECOMBINATION_THRESHOLD`/`_WINDOW_WORDS`/`_WINDOW_STRIDE`/`_BYPASS_THRESHOLD`) в
  коде больше нет — единица-предложение закрывает тот же класс рекомбинации точно, по границе,
  которую поставил автор текста.
- **Корпус** — `GuideCatalog::sections()` (адресами, не замороженным текстом) + `game_tips`.
  `site_posts` исключены (жанр без обязательства актуальности — девблог устаревает по
  построению, `canon_reviewed` не сбрасывается после ребаланса). `community_answers` не входят
  никогда ни при каких обстоятельствах — инвариант анти-храповика: корпус, питаемый собственным
  выходом бота, дрейфует без верхней границы.
- **Рубеж формы держит вето** там, где провенанс его лишился — четыре правила R1-R4, любое
  достаточно: R1 союз сопоставления (по союзам, не единообразно — «чем» требует сравнительной
  степени в том же ответе, «нежели»/«вместо» безусловны, «а не» безусловен кроме уточнения
  «а не только/просто/лишь»); R2 корень оценочного сравнения без союза («Лаборатория
  полезнее»); R3 рекомендательный оборот + инфинитив («стоит делать X»); R4 — зеркало R3,
  сравнительная степень + условие-действие с инфинитивом («попадаются чаще, если идти без
  брони»). Детектор сравнительной степени — одна функция, общая для R1 и R4, не два независимых
  списка. Ключуется на союз/корень/оборот, не на список прилагательных — список в русском не
  закрывается. Пятое правило (R5, клауза-императив + маркер оптимизации по времени/моменту)
  существовало между story 73 и story 78 и было отменено целиком (ADR-178, поправка №5) — денило
  описательные фразы про механику («Уровень здоровья восстанавливается, пока ты на базе») по
  случайному совпадению позиции подлежащего-существительного с позицией ожидаемого
  глагола-императива, и обходилось перефразом тем же автором, против которого было заведено.
  Стоп-правило ADR-178 действует и после отмены R5: новые правила формы не добавляются без
  замеренного класса утечки на живом чате — пример из выдуманной выборки классом не считается.
- **Лексический стоп-лист и числительные словами сверяются по границе слова**, не по
  подстроке — раньше «сто» ловило «ме**сто**»/«**сто**ит», «три» — «ос**три**ё», «один» —
  «**один**аково»; инфикс-фрагменты составных числительных («дцать», «десят») — единственное
  осознанное исключение, они никогда не бывают отдельным словом. Допуск на падежные/спрягаемые
  окончания читается ПО ФОРМУЛЕ от длины стема (≤4 буквы → 1, ≥5 → 3), а не по плоскому числу —
  story 79 нашла, что плоский default молча снял длинные числительные («процент», «тысяч»,
  «миллион») с их падежных форм именно потому, что формула их и так покрывала без отдельной
  записи в карте исключений; карта (`STEM_OVERRIDES`/`TOLERANCE_OVERRIDES`) держит только слова,
  где формула ошибается в любую сторону (спрягаемые глаголы с чередованием гласной — «упирается»
  → «упираются» — формуле не хватает; неизменяемые слова вроде «сто»/«достаточно» — формула
  дала бы лишку и поймала бы посторонние «стол»/«достаточность»).
- **Провенанс — пометка, режимы `community.guard.provenance_mode`**: `advisory` (default) —
  непокрытое предложение получает `advisories` (текст + адрес лучшего источника + ratio), но
  `verdict()` всё равно `allow`; `deny` — откат к старому вето без деплоя; `off` — рубеж не
  считается вовсе. Порог `community.guard.provenance_threshold` (default 0.65) больше не решает
  allow/deny — регулирует только шумность пометки.
- **На отправке провенанс не считается никогда** (`CommunityAutoReplyHandler`, story 57) — корпус
  дрейфует между одобрением и отправкой, пересчёт на отправке означал бы тихую смерть
  одобренного ответа от правки справочника. **Одобрение непокрытого ответа требует второго
  подтверждения** в `/admin/community` (story 68) — форма называет непокрытые предложения
  поимённо, факт подтверждения уходит в `admin_audit_log`.
- **Инвариант чтения:** пометка есть → предложение сомнительно; пометки нет → **не значит
  ничего** (признак двумодален, не разделяет классы). На замороженном корпусе-эталоне
  (`tests/_support/Community/provenance-corpus.php`, срез 2026-08-26) 4 из 22 несравнительных
  фабрикатов проходят вовсе без пометки — **оценка снизу**, а не измеренная величина: фикстура
  снята с локальной БД (60 фрагментов: 32 `/guide` + 28 `game_tips`), а боевой корпус спека
  называет числом 133 — больший корпус даёт больше случайных лексических совпадений, значит
  реальная доля непомеченных фабрикатов на проде не меньше этой.
- **Измеренный ложный отказ рубежа формы на эталоне /guide (32 фрагмента) — 6.2% (2/32) после
  R1–R4** (story 78, подтверждено story 79): сравнительное обещание про телепорт в `/guide` —
  «…дорога домой начинает съедать больше времени, чем сама вылазка» (`guide:teleport`) и
  уточнение в `guide:npc` («…можно говорить, а не только драться») — находки о справочнике, не
  о гварде, лечатся правкой источника, не описаны здесь как задача этой story. Эталон /guide —
  связная проза справочника, структурно не похожая на реальный ответ бота; **отдельно измерен
  коридор на выборке из 14 коротких ответов в тоне владельца — 7.1% (1/14)** (story 78), где
  единственное совпадение — легитимный улов R3 («стоит + инфинитив»), не ложное срабатывание;
  ставка не заметно выше прозы, эскалация архитектору по стоп-правилу story 78 не потребовалась.
  R5 (клауза-императив + маркер оптимизации) существовал между story 73 и story 78 и добавлял
  1/32 ложного срабатывания на личную форму глагола («выбираешь», `guide:march`) — устранён
  вместе со всем правилом при его отмене (ADR-178, поправка №5), не является текущим числом.
- Tip/guide-вердикты по всей волне (ADR-177 §Последствия, ADR-178 §Последствия) уже вынесены и
  переоценки не требуют: **совет — нет** (изменение внутреннее, игроку видно только как «бот
  реже/иначе отвечает автоматом»); **раздел `/guide` — нет** (та же причина, новой player-
  поверхности нет).

Источники: [[decisions/ADR-177-Community-guard-provenance-unit|ADR-177]] (единица провенанса,
корпус, запрет формы, методика замера), [[decisions/ADR-178-Community-guard-provenance-advisory|ADR-178]]
(потеря права вето; поправка №2 — правило R4; поправка №3 — R1 по союзам, общий детектор
степени; поправка №4 — вырез «а не только/просто/лишь» отменён, правило R5 добавлено, стоп-правило
на дальнейшие правила формы; поправка №5 — R5 отменён целиком, класс признан непокрываемым).

**3. Гвард на входящем + лексический слой.**
Утечку несёт не только ответ, но и **вопрос**: на «уворот же упирается в 75, да?» любой
ответ, включая уклончивый, читается чатом как подтверждение или опровержение — бот работает
оракулом с одним битом на выход, и бита достаточно. Поэтому:
- вопрос содержит число, диапазон, «%», «потолок/максимум/порог/кулдаун» или форму проверки
  гипотезы («правда, что…», «…, да?») → авто-режим выключен независимо от текста ответа;
- лексический стоп-лист на исходящем: быстрее / выгоднее / оптимально / всегда / никогда /
  достаточно / упирается / потолок / порог / перестаёт / бесполезно / окупается + числительные
  словами («сорок процентов», «вдвое», «раз в сутки»);
- односложные подтверждения («да», «нет», «верно», «почти») запрещены как класс ответа на
  вопрос о механике;
- счётчик агрегации: N вопросов об одной механике от одного игрока или в одном топике за
  окно → эскалация, а не одиннадцатый ответ. Десять невинных реплик складываются в карту.

**4. Стоп-темы — уточнённые.**
Добавить: точная отдача дронов и роботов; всё про обнаружение (радиус, окно, кто кого видит);
окно неуязвимости после респавна; карма, спред и арбитраж; формула налога от числа баз;
координаты и «где спавнится редкое»; модель RNG и всё похожее на дюп; распределение статов;
**dormant и незапущенные фичи**; сроки, планы, вайпы; данные другого персонажа; устройство
самого бота и его инструкции (защита от инъекции через чат).

Сузить, потому что рубит полезное:
- «пороги смерти» целиком — вредный запрет: качественный ответ («смерть не откат, теряешь
  часть опыта и статов, вещи с базы безопаснее») напрямую снижает отвал новичков. Резать
  проценты, а не тему.
- «PvP-тайминги» слишком широко: пауза между атаками — анти-спам, а не чит.
- «как быстрее всего» → «быстрее всего прокачаться / нафармить / обогатиться», иначе зарежет
  главный вопрос новичка «что делать дальше», ради которого бот и заводится.
- «подозрение на баг» — **не молчать**: молчащий бот убивает канал баг-репортов. Утверждённая
  квитанция «записал, передал» + эскалация, без обсуждения механики.

**5. Live vs dormant — рубеж, которого не было.**
Белый список, гвард и стоп-темы не ловят главную ложь: механика описана верно и без чисел,
но **выключена килсвитчем**. Банк замораживает текст на месяцы, а `GameSettings` флипается
из админки за 60 секунд. Решение: у записи банка обязательное поле `requires_setting`
(ключ килсвитча, nullable). Перед отправкой читается `GameSettings`; выключено → ответ не
уходит, вопрос падает в эскалацию. Ответ, упоминающий любую из ~30 подсистем с килсвитчем,
без заполненного `requires_setting` в банк не принимается — тестом, не на честном слове.

**Плюс два срока годности.** У каждой записи банка — дата утверждения; старше 90 дней уходит
из авто-режима в ревью. И кнопка отзыва: «убрать из банка + отправить поправку в тот же топик».
Без неё единственный способ починить ошибку — искать её глазами.

## 6. Ответ бота = обещание студии

Фильтр на этапе одобрения: **«если это скриншотят через полгода, оно всё ещё правда?»**
Запрещено как класс, детерминированной проверкой:

| Класс | Маркеры |
|---|---|
| Обещания срока | скоро, в ближайшем обновлении, уже чиним, в планах |
| Гарантии | гарантированно, точно будет, не потеряешь, всегда работает |
| Признание бага от лица студии | это баг, это сломано, известная проблема |
| Компенсации и выдача | вернём, компенсируем, начислим, восстановим |
| Оценка игрока | он неправ, это читерство, его накажут |
| Речь от лица владельца | мы решили, разработчик сказал, наша позиция |
| Отрицание проблемы | всё работает нормально, у других не воспроизводится |
| Точные числа баланса | проценты, шансы, пороги, кулдауны, дроп |

Позитивная замена одна на все случаи: **«Не знаю точно. Передал вопрос разработчику.»**

**Отказ без маршрута — тоже запрещённый класс.** Любой отказ обязан нести, куда идти:
экран, кнопка, раздел. «Не скажу» без «зато вот где смотреть» не проходит гвард. Доля
отказов — метрика на `/admin/community`, а не побочный эффект: её рост означает «пополнить
банк», а не «гвард работает».

**Тон.** Регистр Роби задан существующими текстами: на «ты», короткий императив, постапок,
без канцелярита и без мета-слов. Эталон:

> Верстаки и их уровни держу в «📖 Путь новичка» → Крафт — там же сказано, как поставить.
> Загляни, это быстрее, чем я перескажу.

Канон-риски, где Роби соврёт легче всего (каждый — уже случившийся в проекте инцидент):
верстаки («2 верстака / 3 уровня», ADR-130 — фразы «поставь верстак 2» не существует),
имена построек (`buildings.name_ru` против `name_rus` даёт английский fallback),
фракции («Партизаны» канон, «Разбойники» — только SEO-мост в старых постах),
PvP-дуэли / транспорт / Оракул — все за килсвитчами.

## 7. Приватное не выносится на публику

Бот **не отвечает публично** и уводит в личку владельца: жалоба на игрока, обвинение в
читерстве или скаме; потеря прогресса, спор о выдаче; всё, где фигурирует чужой ник или
скриншот чужого профиля; подозрение на баг (публичный ответ = публикация эксплойта); личные
данные; эмоциональная эскалация. Уводит **одной репликой, не пересказывая суть вопроса** —
«По поводу того, что X тебя ограбил…» уже конфликт.

## 8. Модерация — минимум, и сразу в shadow

Не полноценный антиспам, но один блок обязателен с первого релиза: **ссылки и вербовка**,
потому что это единственная угроза, бьющая по студии, а не по настроению. Новичок в чате
шлёт ссылку / чужого бота / forward из чужого канала, либо «продам аккаунт», «прокачаю за» →
удалить + сигнал владельцу в личку. Не банить: удаление обратимо, бан нет.

Токсичность, мат и ссоры бот **не трогает вообще** — на паре сотен человек автомодерация тона
всегда даёт ложные срабатывания, и первое «за что удалили» стоит дороже самого мата.

Килсвитч `community.moderation.mode` = `shadow` первую неделю: бот только пишет владельцу
«я бы удалил вот это», ничего не удаляя. В боевой — только после недели с нулём ложных.

## 9. Приватность хранения

Хранение сырого текста — 30 дней, по образцу firehose ADR-148, отдельной cleanup-командой.
Таблицы классифицируются в `WipeManifest`. Объявление идёт **за сутки до входа бота**, от
лица владельца, а не бота — возмущение рождает не факт хранения, а то, что о нём узнали
задним числом. Закреп называет три вещи, которые гасят почти всё: срок (30 дней, не «навсегда»),
границу (только этот чат, личные переписки не видны), выход (просьба на удаление выполняется).
Обещание про удаление даётся только если админ-кнопка «стереть всё от этого игрока» реально
существует. Логи бот не цитирует в ответах: «Роби процитировал, что я писал месяц назад» —
скандал на ровном месте.

## 10. Канал прод <-> локаль — решено (ADR-176)

**SSH + две spark-команды**, публичного API не будет:
`php spark community:export --since=<id>` (JSON в stdout) и `php spark community:import` (stdin).
Дёргает локальный Git Bash-хук по уже существующему ключу `~/.ssh/wildworld_deploy`.

Почему не HTTP-пара за секрет-заголовком: профиль канала — раз в сутки-двое, десятки строк,
человек с обоих концов; постоянная анонимно достижимая поверхность 24/7 ради двух вызовов
в день несоразмерна. Утечка SSH-ключа не создаёт нового худшего случая — это уже шелл с
`.env`, токеном бота и БД; утечка bearer-секрета была бы **отдельным** классом компрометации,
который надо отдельно сторожить и ротировать. Аутентификация по ключу заодно снимает replay,
перебор и потребность в собственном rate-limit. Откат дешевле: удалить две команды против
«удалить опубликованный маршрут».

**Главный риск — не транспорт, а `push`:** через него на прод заезжает текст, который бот
скажет игрокам от своего лица. Защищает его инвариант, а не канал: **импорт создаёт только
`draft`**; путь draft -> сказано в чат идёт исключительно через `/admin/community` с `audit()`.
Импорт не имеет права трогать строки в статусах «одобрено» и «отклонено».

**Контракт формата:** курсор `--since=<id>` (не offset), лимит 200 на вызов, потолок 1000;
`state.json` с последним id; stdout — только JSON, диагностика в stderr, ненулевой exit code
при сбое; вывод в файл, а не в транскрипт. Push: не более 100 черновиков и 256 КБ на вызов,
текст черновика до 3500 символов (до 1024, если пойдёт подписью к фото), `client_key` = ULID
генерится локально, UNIQUE-индекс на нём даёт идемпотентность повторного push.

## 11. Волны и story

**Волна 1 — предусловия (бот ещё молчит).**
- `01` Гейт по типу чата в вебхуке + rate-limit перестаёт душить игрока групповым трафиком.
- `02` Схема: `community_messages` (`WipeManifest: TRANSIENT`), `community_answers` (`KEEP` — авторский корпус наравне с `game_tips`; + `requires_setting`, `client_key` UNIQUE, `status` default `draft`, `approved_at`, `revoked_at`), модели.
- `03` Килсвитчи, пороги и лимиты в `GameSettings` (одна seed-миграция).
- `04` Канон голоса Роби: лор-нота + glossary в vault, `Config\CommunityVoice` с утверждёнными фразами (отказ, квитанция, «не знаю», приветствие, мета-вопрос), тест на написание имени.

**Волна 2 — приём, голос, гвард.**
- `05` `CommunityIngestService`: запись, топик/тред, детектор вопроса, признак адресного обращения, анти-флуд по автору.
- `06` `CommunityChatSender`: отправка в `chat_id`+`message_thread_id`, реплай, `sendMessageReaction`, лимиты, аудит.
- `07` `CommunityGuard`: провенанс предложений, лексический стоп-лист, гвард на входящем, `requires_setting`, канон-словарь.

**Волна 3 — мгновенный слой.**
- `16` Связка: вебхук зовёт приёмник (добавлена по ходу сборки, см. §Descoped).
- `08` Банк и матчер: нормализация, два порога (A/B), кулдауны по топику и автору, «человек уже ответил», склейка дублей.
- `09` Task-handler авто-ответа + регистрация в `Tasks.php`.

**Волна 4 — модерация и канал.**
- `10` Модерация ссылок и вербовки в режиме `shadow`.
- `11` `app/Commands/CommunityExport.php` + `CommunityImport.php` по контракту ADR-176.

**Волна 5 — руки владельца.**
- `12` `/admin/community`: очередь, одобрить / правка / отклонить / отозвать, удаление данных игрока, метрики (доля отказов, «бот против живых», топ повторяющихся вопросов).
- `13` Локальная синхронизация: SessionStart-хук тянет inbox в `.claude/community/`.

**Волна 6 — конвейер черновиков.**
- `14` Скилл `/community`: черновики + редколлегия (лор-хранитель, гейм-дизайнер, анти-спойлер, редактор).

**Волна 7 — бумага и покрытие.**
- `15` Вердикты guide/tips, раздел `/guide`, совет, tech-writing, текст закрепа, чек-лист обкатки.

## 12. Обкатка: первые две недели

- **День −1.** Объявление владельца + закреп. Сутки на вопросы — до включения, а не после.
- **День 1–3.** Роби входит **немым**: приём, хранение, детект вопросов, очередь, модерация
  в `shadow`. Отвечает ноль раз. Банк собирается на реальных вопросах, а не на выдуманных.
- **День 4–5.** 15–25 ответов в банк. Меньше 15 — рано включать; больше 30 сразу — почти
  наверняка есть выдуманные.
- **День 6.** Первый голос **узко**: один топик, потолок 5 ответов в час, выдержка 60–90 сек.
  В General и разговорных топиках молчит.
- **День 7–10.** Расширение по одному шагу за раз. Никогда два изменения сразу.
- **День 10–12.** Модерация из `shadow` в боевой — только при нуле ложных за неделю.
- **День 13–14.** Ревизия всех отправленных ответов + первый замер метрики «бот против живых».

**Сигналы отката** (любой → `community.autoreply.enabled = false`, не «подкрутить»):
неверный публичный ответ **дважды**; бот перебил живой ответ; публичный ответ на приватную
тему — откат с одного случая; «заткни своего бота» от двух разных людей; живые перестали
отвечать друг другу; ложное удаление нормального сообщения — модерация обратно в shadow.

Ручка отката должна работать без релиза и быть проверена **до** дня 6.

## 13. Метрики, по которым видно провал

- **Доля сообщений, на которые отвечает бот, а не другой игрок.** Если игроки перестали
  отвечать друг другу — бот победил, а чат умер. Считать с первой недели.
- Доля отказов гварда — сигнал пополнить банк.
- Открытый вопрос без ответа дольше 72 часов — инцидент, не «низкий приоритет».
- Топ повторяющихся вопросов — готовая карта дыр онбординга, вход в guide/tips-ревью.

## 14. Что шипим первым

Волны 1–3 — самостоятельный релиз: бот в чате, слышит всё, отвечает только заранее
утверждённым текстом, с гвардом. Волны 4–7 добавляют модерацию, канал и конвейер черновиков.
Если после волны 3 окажется, что банка из 20–30 ответов хватает на большую часть чата,
объём волн 6–7 можно урезать.

## 15. Открытые допущения (Закон 1)

- Режим приватности бота в группе (`/setprivacy` → Disable у BotFather) придётся выключить,
  иначе бот увидит только обращения к себе. Действие руками, вне кода.
- `allowed_updates` на стороне Telegram сейчас неизвестен; после включения группы фиксируется
  версионно spark-командой, а не разовым curl.
- Пороги матчера, выдержка и потолки задаются вслепую; первые значения заведомо осторожные,
  калибруются по первому срезу.
- Связка «участник чата ↔ персонаж» (бот видит `from.id`, игра знает игрока) в волнах 1–7
  **не реализуется**. Игрок в панели назвал это главным отличием от любого FAQ-бота, но это
  отдельный объём: ответ, учитывающий уровень и открытые гейты, требует своей модели данных.
  Вынесено сознательно — решение владельца, включать ли отдельной волной.
- Число записей в `game_tips` из репозитория не подтверждается (советы приезжают миграциями,
  часть dormant) — считается `SELECT COUNT(*)` на проде в волне 1.
- `.claude/community/` обязан попасть в `.gitignore`: там сырой текст чата игроков.
- **Отдельной задачей вне этой спеки (Закон 3):** легаси-ветка `BotController.php:47-54` —
  отсутствующий `telegram.WEBHOOK_SECRET` сейчас ПРОПУСКАЕТ проверку вместо 403. ADR-176
  объявляет этот шаблон запрещённым, но чинить его внутри community-спеки нельзя.

## 16. Решения владельца при утверждении (2026-08-25)

Владелец делегировал три открытых вопроса Queen. Зафиксированы так:

1. **Буквальное «работает только когда я запускаю Claude Code» — не выполняется дословно.**
   Выдача утверждённого банка работает круглосуточно; к сессии привязаны только генерация
   черновиков и утверждение. Буквальное прочтение дало бы мёртвый чат между сессиями, а
   безопасность обеспечивает не привязка к сессии, а то, что каждое слово утверждено заранее.
2. **Модерация ссылок и вербовки (story 10) — в объёме**, режим `shadow` с первого дня.
   Мат и токсичность бот не трогает намеренно.
3. **Политика хранения (§9) — в объёме**: 30 дней, объявление за сутки до входа бота,
   кнопка удаления данных игрока по запросу.

## Descoped
<!-- одна строка на каждое сужение по ходу сборки: что срезано, почему, какие данные это показали -->

- **Добавлена story `16` (волна 3) — дефект планирования, не сужение.** Story 01 создала
  `handleCommunityUpdate()` пустым, story 05 по Non-goals не имела права трогать контроллер —
  связку не получил никто, и приём оказался бы BUILT-BUT-DEAD: написан, покрыт тестами, не
  вызывается ниоткуда. Найдено на приёмке story 05 по её собственной оговорке. Теперь
  волна 3 — это `08` (матчер) и `16` (связка), файлы непересекающиеся.

## 17. Ревью-гейт 2026-08-25 — BLOCK от обоих ревьюеров

Слепая приёмка: **ACCEPTED** (10 просьб брифа выполнены, BROKEN none) — но она судит
«делает ли собранное то, что просили», а не «правда ли то, что оно утверждает».

`lead-review` и второй ревьюер на другой модели независимо дали **BLOCK**. Обе критические
находки подтверждены собственным прогоном Queen, а не приняты на слово:

1. **Провенанс не держит на боевом корпусе.** Против фактических 32 разделов `GuideCatalog`
   шесть выдумок из шести проходят все пять рубежей, включая дословный пример утечки из
   §5.2 («Лаборатория выгоднее Мастерской», переформулированный в обход стоп-листа), и
   включая «Да, верно.» / «Ага.» / «Именно так.».
   🔴 **Запись «Приёмка Queen — враждебная проба рубежа 1… Вывод: дыры нет» в story 07
   недействительна.** Проба ставилась против ОДНОГО узкого фрагмента про дрона. На таком
   корпусе она воспроизводится, на боевом — нет. Проверка была сделана на конфигурации,
   в которой система не работает, и объявлена доказательством. Story 21 чинит.
   🔴 **Закрыта волной 9 (story 63/65/69/70), не story 21.** Story 21 сузила порог и
   исправила стемминг, но не изменила замысел — на боевом корпусе провенанс продолжал
   пропускать бо́льшую часть выдумок (82.8% в замере story 63) до тех пор, пока не
   поменялась единица подтверждения (документ → предложение, story 63) и не выяснилось
   измерением (не догадкой), что у самого признака нет порога, разделяющего «правда» и
   «выдумка» (ADR-178): законный пересказ и лучший фабрикат перекрывались по значению.
   Решение — не более строгий порог, а смена роли: провенанс лишён права вето и стал
   пометкой для ревьюера (ADR-178), рубеж формы (R1-R4, story 63/70) взял на себя вето.
   Калибровка судит зафиксированный корпус-эталон, а не то, что оказалось под рукой
   (story 65) — измеренный ложный отказ рубежа формы 3.1% (1/32), фабрикатов без пометки
   4/22. Подробности — §5.2 выше.
2. **Полоса A мертва для «Роби, …»** — фильтр «обращение к конкретному человеку» глотает
   имя самого бота, а заодно «Народ, а где вода?» и «Ребят, как крафтить?». Проверено
   прямым прогоном регулярки. Story 20 чинит.
3. **Ретенция 30 дней обещана публично и не существует в коде** — у ключа нет читателей;
   плюс строки `new` не закрываются никогда и очередь тика растёт монотонно. Story 22 чинит.

Плюс восемь серьёзных: сигнал модерации теряется на легаси-Markdown ровно на скам-ссылках;
правка сообщения обходит модерацию и приём; `channel_post` просачивается в игровой
диспетчер; окно двойной отправки; вечный ретрай забивает журнал, по которому считаются
потолки; часы PHP против часов БД в потолках; фаза 5 скилла импортирует в локальную БД;
очередь админки показывает весь чат как открытые вопросы; главная метрика считает ручные
ответы владельца ответами бота.

**Вердикт приёмки после добавления волны 8 недействителен** — она судила пакет из 19 story,
которого больше нет. Переспросить после ремонта.

### Урок, который дороже остальных
Восемь story из девятнадцати вернулись с дефектами при зелёных тестах, и оба ревьюера
сошлись на одном: гейты были зелёными **на сломанном**. Тест `testRobiPrefixMarksAddressedToBot`
подаёт ровно сломанный случай и утверждает только половину; добавление одной строки
краснит его сегодня, без правки кода. Тест рубежа 1 гоняется на самодельном корпусе из трёх
фрагментов и не может покраснеть на реальной деградации. Приёмочная проба Queen повторила
ту же ошибку в story 07.


## 18. Ревью-гейт волны 9 (2026-08-26) — BLOCK

Слепая приёмка: **ACCEPTED** (все просьбы брифа выполнены; отдельно подтвердила, что смена
архитектуры анти-чит-защиты цель брифа не ослабила). Второй ревьюер: **PASS**. Первый ревьюер:
**BLOCK**. Из 12 находок §17 закрыто десять, две частично.

Ниже — то, что подтверждено **собственным прогоном Queen** на живом гварде, а не принято на слово.

### 🔴 Критическое 1 — рубеж формы обходится двумя символами

Story 70 вывела из-под R1 связку «а не **только / просто / лишь**». Это открывает ровно тот класс
советов-замен, который ADR-178 поправка №3 назвала запрещённым к открытию:

```
ALLOW  Ставь Лабораторию, а не просто Мастерскую.
ALLOW  Качай выносливость, а не только силу.
deny   Иди в лес, а не в горы.   [comparative_claim]
```

Различитель не может быть голым списком из трёх слов после союза: уточнение («можно говорить,
а не только драться») обязано проходить, сопоставление двух вариантов через тот же оборот —
отклоняться. Это единственный рубеж, у которого после ADR-178 осталось вето.

### 🔴 Серьёзное 2 — две системы ключей сведены без моста, тест зелен на вымысле

`community_messages.telegram_user_id` — сырой `from.id` Telegram (`CommunityIngestService.php:185`).
`characters.telegram_user_id` — внутренний `telegram_users.id` (джойн `CharacterModel.php:189`,
конвенция зафиксирована в докблоке `PlayerActionLogger`). `WipeService::telegramUserIdOf()`
подставляет второй в связку, объявленную по первому → `resetCharacter()` удаляет **ноль** строк
и рапортует успехом. Полный вайп не затронут.

`WipeServiceCharacterResetTest` не краснеет, потому что сеет обе стороны как `1001` — то есть
кодирует ошибочное предположение. Урок проекта `feedback_two_key_systems_need_an_explicit_bridge`
описывает ровно этот класс; тест обязан различать внутренний id и сырой Telegram-id.

### 🔴 Серьёзное 3 — план обещает больше, чем держит гвард

Совет в **повелительном наклонении** — самая естественная форма лайфхака — не покрыт ни одним из
R1–R4: все четыре ключуются на сравнение, оценку и рекомендательный оборот.

```
ALLOW  Собирай ресурсы ночью — охрана спит.
ALLOW  Оставляй вещи на базе перед вылазкой — тогда смерть почти ничего не отнимает.
ALLOW  Заходи в чужую базу, пока хозяин в походе.
```

Стоп-темы фразо-буквальны и мимо естественных формулировок вопроса. Реально утечку держит одно:
авто-режим отдаёт только текст со `status='approved'`, то есть **человек**. ADR-178 это признаёт
(«гвард не проверяет правду»), §5.2 утверждает сильнее. План обязан назвать, какой класс держит
гвард, а какой — только одобрение владельцем.

### Прочие серьёзные

- **`deny`-режим режет отправку** вопреки ADR-178 (вето обещано только на одобрении); утверждение
  размножено в четырёх местах: `plan.md`, докблок гварда, `effect_text` миграции, tech-writing нота.
  → story 72, в работе.
- **Стоп-лист потерял падежные формы.** Граница слова с «максимум одной лишней буквой»: «потолок»
  ловится, «потолка» / «потолки» / «порогов» / «сотни» — нет. Ни один тест не краснеет.
- **Очередь админки разошлась с выборкой тика.** Тик берёт `is_question=1 OR addressed_to_bot=1`,
  очередь — только `is_question=1`: «Роби помоги» эскалируется, но владельцу не видна.
- **Эталон снят с локальной БД** (60 фрагментов), а боевой корпус — 133. Замер ложного отказа
  (3.1%) от этого не страдает, а «4 из 22 без пометки» — оценка снизу, и в плане/ADR она названа
  измеренной величиной.

### Мелкие

- `.claude/skills/community/SKILL.md` всё ещё называет источниками `site_posts` и `glossary` —
  первые выкинуты ADR-177 §3, вторые в корпус не входили никогда.
- Двухшаговое одобрение работает серверным гейтом, но пометку получает большинство добросовестных
  пересказов — второй клик станет рефлексом. Привыкание к предупреждению.
- `ingestEditedMessage()` не переоткрывает строку, закрытую TTL-чисткой; вероятно осознанно, нигде
  не записано.
- Опечатка «выуживающий чити» в `CommunityChatSender.php:487`.
