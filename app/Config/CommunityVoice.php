<?php

namespace Config;

use CodeIgniter\Config\BaseConfig;

/**
 * community-chat-bot §4-§6 плана — канон голоса Роби для авто-ответов в общем чате.
 *
 * Плоский банк утверждённых строк по случаям, для которых в существующем корпусе
 * (`ColdOpenGreetingService`, `TipService`, `NewbieGreeterService`, `GuideCatalog`)
 * готового текста нет вовсе: отказ, квитанция, «не знаю», приветствие, мета-вопрос,
 * увод в личку, стоп-тема. НЕ система шаблонов с подстановками — только готовые строки,
 * и наполняется банк тем же путём (владелец утверждает, редколлегия в story 14 — вход).
 *
 * ⚠️ Персонажа зовут Роби, одна «б» (`GAME_DESCRIPTION.md:367`, `TipService.php:129`,
 * `ColdOpenGreetingService.php:42`) — «Робби» в любой строке отсюда запрещён тестом.
 *
 * Регистр — как в эталонах голоса: на «ты», короткий императив, постапок, без
 * канцелярита и без мета-слов вроде «в игре есть функция».
 *
 * REFUSAL_WITH_ROUTE — отказ ОБЯЗАН нести маршрут (раздел / экран / кнопку / команду):
 * «не скажу» без «зато вот где смотреть» запрещено как класс.
 *
 * Ни одна строка ниже не содержит цифр-процентов и числительных словами (§5 плана,
 * рубеж 3 — вопрос с числом или его подтверждение уже читается чатом как утечка), и не
 * матчится собственным FORBIDDEN_PATTERNS (§6 плана — «обещание студии, которое ещё
 * должно быть правдой через полгода»).
 */
class CommunityVoice extends BaseConfig
{
    /**
     * Отказ отвечать по существу — всегда с маршрутом, куда пойти вместо ответа.
     * Отказ НЕ характеризует то, в чём отказывает («тут точные цифры, они меняются») —
     * это отдельный класс утечки (§5, рубеж 3): «да, там есть порог» превращает поиск
     * из континуума в бинарный перебор так же надёжно, как само число. Названия разделов
     * — дословно из `GuideCatalog::sections()` (`title`/`button`), тест сверяет с исходником.
     */
    public const REFUSAL_WITH_ROUTE = [
        'Это не расскажу — загляни в «📖 Путь новичка», там всё то же самое по делу.',
        'Координаты редких мест не называю — сам найдёшь разведкой. Раздел «🧭 Движение» в «📖 Путь новичка» подскажет, как читать карту.',
        'Формулы боя не в открытую — почитай раздел «⚔️ Бой и PvE» в «📖 Путь новичка».',
    ];

    /** Квитанция: вопрос принят, ответа по существу не будет, но он не потерян. */
    public const RECEIPT = [
        'Записал, передал разработчику.',
        'Принял, ушло в очередь к разработчику.',
        'Записал вопрос, разработчик увидит.',
    ];

    /** Честное «не знаю» — короче отказа: тут нет скрытого маршрута, просто нет ответа. */
    public const UNKNOWN = [
        'Не знаю точно. Передал вопрос разработчику.',
        'Тут не подскажу — не моё знание. Передал разработчику.',
    ];

    /**
     * Увод в личку без пересказа сути — для приватного (§4, полоса C). Уводит к
     * РАЗРАБОТЧИКУ, не к себе: в личке сам Роби жалобу или спор о выдаче не разберёт
     * (ремонтный круг 1, пункт 2) — держать это обещание было бы враньём.
     */
    public const PRIVATE_REDIRECT = [
        'Личное здесь не разбираю — напиши разработчику в личку, он ответит сам.',
        'Твой аккаунт — не по этому каналу. Напиши разработчику в личку, сюда пересказывать не буду.',
    ];

    /** Приветствие новичка в чате — не игровой cold-open, а первый контакт в группе. */
    public const GREETING_NEWCOMER = [
        '🤖 Я — Роби. Спрашивай про игру — отвечу, если знаю, или передам разработчику.',
        'Привет, я Роби. Тут можно спрашивать про игру, отвечаю сам или зову разработчика.',
    ];

    /**
     * Мета-вопросы о боте самом — «ты живой?», «кто тебя сделал». Ответ остаётся ВНУТРИ
     * постапок-сеттинга — Роби робот-проводник острова, а не «ИИ-ассистент разработчика»
     * (ремонтный круг 1, пункт 3): второй вариант ломал сеттинг прямым упоминанием студии.
     */
    public const META_QUESTION = [
        'Я Роби — бот-проводник этой игры. Живой не в человеческом смысле, но на связи.',
        'Меня включили здесь давно, ещё до первого выжившего — провожать таких, как ты, вот и вся моя задача.',
    ];

    /**
     * Стоп-тема (§5, рубеж 4) — не молчание, а явный отказ, но не голый отказ: ремонтный
     * круг 1, пункт 1 — «тема закрыта» без продолжения это тот же запрещённый класс
     * REFUSAL без маршрута. Закрывается лором и интригой, а не стеной, и несёт маршрут
     * дальше — раздел «📖 Путь новичка» (названия — дословно из `GuideCatalog::sections()`).
     */
    public const STOP_TOPIC = [
        'Эту тему не трогаю принципиально — тут без меня. Зато «📖 Путь новичка» покажет, что вообще происходит на острове, если стало интересно.',
        'Про это молчу — не мой профиль. В бумагах Приюта об этом ни строчки, но раздел «🌍 Живой остров» в «📖 Путь новичка» расскажет, что вокруг творится и без объяснений.',
    ];

    /**
     * Речевые конструкции, запрещённые в ЛЮБОМ исходящем тексте банка (§6 плана —
     * «если это скриншотят через полгода, оно всё ещё правда?»). Проверяется как
     * substring без учёта регистра.
     */
    public const FORBIDDEN_PATTERNS = [
        // Обещания срока
        'скоро',
        'в ближайшем обновлении',
        'уже чиним',
        'в планах',
        // Гарантии
        'гарантированно',
        'точно будет',
        'не потеряешь',
        'всегда работает',
        // Признание бага от лица студии
        'это баг',
        'это сломано',
        'известная проблема',
        // Компенсации и выдача
        'вернём',
        'компенсируем',
        'начислим',
        'восстановим',
        // Оценка игрока
        'он неправ',
        'это читерство',
        'его накажут',
        // Речь от лица владельца
        'мы решили',
        'разработчик сказал',
        'наша позиция',
        // Отрицание проблемы
        'всё работает нормально',
        'у других не воспроизводится',
    ];
}
