<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * ADR-150 (чистка дублей «События») — правда пути в совете `EventHistory`.
 *
 * Совет звал: «Открой *«🎉 События»* **в нижнем меню**». Кнопки «События» в нижнем меню не было
 * НИКОГДА: экран жил только кнопкой, скопированной на 17 чужих экранов. После чистки у него есть
 * канонический дом — экран «🌍 Мир» (события про состояние острова, а не про персонажа).
 *
 * Совет, обещающий несуществующий путь, — тот самый баг, из-за которого появилось правило
 * UX-DISCOVERABILITY. Чиним в той же задаче (TIPS-COVERAGE, ADR-134): нового совета не заводим,
 * чиним существующий — дубль-совет про переезд кнопки был бы шумом.
 *
 * Идемпотентно: UPDATE по `title_en`, текст переписан целиком. markdown-safe (парные `*`),
 * media-off (весь смысл в тексте), tip_type не трогаем.
 */
class Adr150FixEventHistoryTipPath extends Migration
{
    private const TITLE_EN = 'EventHistory';

    private const NEW_CONTENT = '🌟 *Мировые события* налетают на пустошь сами — буря, метеоритный дождь, '
        . 'нашествие. Многие из них *локальные*: задевают только тех, кто сейчас в затронутых биомах, '
        . 'а не всех сразу — поэтому соседа бьёт, а тебя нет. У каждого события есть начало и конец, '
        . 'оно не навсегда. Нажми *«🌍 Мир»* внизу экрана, а там — *«🎉 События»*: вверху что идёт прямо '
        . 'сейчас (где, какой эффект, сколько осталось и коснулось ли тебя), ниже — *история последних '
        . 'событий* с началом, концом и длительностью. Если здоровье просело, а активных событий уже нет — '
        . 'загляни в историю: скорее всего, тебя только что задело завершившееся событие. Опасное можно '
        . 'переждать на базе или держать при себе защитный предмет события.';

    private const OLD_CONTENT = '🌟 *Мировые события* налетают на пустошь сами — буря, метеоритный дождь, '
        . 'нашествие. Многие из них *локальные*: задевают только тех, кто сейчас в затронутых биомах, '
        . 'а не всех сразу — поэтому соседа бьёт, а тебя нет. У каждого события есть начало и конец, '
        . 'оно не навсегда. Открой *«🎉 События»* в нижнем меню: вверху — что идёт прямо сейчас '
        . '(где, какой эффект, сколько осталось и коснулось ли тебя), ниже — *история последних событий* '
        . 'с началом, концом и длительностью. Если здоровье просело, а активных событий уже нет — загляни '
        . 'в историю: скорее всего, тебя только что задело завершившееся событие. Опасное можно переждать '
        . 'на базе или держать при себе защитный предмет события.';

    public function up(): void
    {
        $this->db->table('game_tips')
            ->where('title_en', self::TITLE_EN)
            ->update(['content' => self::NEW_CONTENT, 'updated_at' => date('Y-m-d H:i:s')]);
    }

    public function down(): void
    {
        $this->db->table('game_tips')
            ->where('title_en', self::TITLE_EN)
            ->update(['content' => self::OLD_CONTENT, 'updated_at' => date('Y-m-d H:i:s')]);
    }
}
