<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * ADR-156 — уточнение совета про Спортзал (засеян в ADR-155, на несколько часов раньше).
 *
 * Совет говорил «Открывается на *5-м уровне*». Формально верно — гейт стройки действительно
 * пропускает с пятого, — но аудит цены показал, что рецепт требует Стекло пакеты (крафт с L25)
 * и Янтарь (добыча с L20), то есть **своими руками зал собирается только к 25-му**. Эмпирика
 * прода это подтверждает: зал стоит у игроков L11, L29, L50, L51, L64, L223 — на пятом уровне
 * его нет ни у кого.
 *
 * Обещать новичку постройку, которую он не соберёт ещё двадцать уровней, — то же переобещание,
 * что «каждые 5ть минут» в описании. Формулировка меняется на честную: кнопка появляется рано,
 * материалы — поздно, и путь к ним назван.
 *
 * Идемпотентно: UPDATE по `title_en`, содержимое задаётся целиком.
 */
class Adr156FixGymTipReachability extends Migration
{
    private const TITLE_EN = 'GymGrowsStrengthPassively';

    public function up(): void
    {
        $content = '🥊 *Спортзал качает тебя сам.* Это единственная постройка, которая растит '
            . '*характеристику* просто потому, что стоит: раз в полчаса она добавляет силу — а сила '
            . 'входит в те четыре числа, из которых складывается твой уровень. Кнопка постройки '
            . 'появляется рано, но собрать зал выходит заметно позже: в рецепте есть редкие материалы '
            . '(их видно на превью постройки — там прямо написано, чего тебе пока не добыть). '
            . 'Сколько даёт именно твой зал — написано на его экране: *«🏠 База»* → *«Спортзал»*; '
            . 'с уровнем зала прибавка растёт. И пока налог за постройку не уплачен, зал молчит.';

        $this->db->table('game_tips')
            ->where('title_en', self::TITLE_EN)
            ->where('content !=', $content)
            ->update(['content' => $content, 'updated_at' => date('Y-m-d H:i:s')]);
    }

    public function down(): void
    {
        $content = '🥊 *Спортзал качает тебя сам.* Это единственная постройка, которая растит '
            . '*характеристику* просто потому, что стоит: раз в полчаса она добавляет силу — а сила '
            . 'входит в те четыре числа, из которых складывается твой уровень. Открывается на '
            . '*5-м уровне*, строится на базе. Сколько даёт именно твой зал — написано прямо на его '
            . 'экране: *«🏠 База»* → постройка *«Спортзал»*; с уровнем зала прибавка растёт. '
            . 'Одно условие: пока налог за постройку не уплачен, зал молчит.';

        $this->db->table('game_tips')
            ->where('title_en', self::TITLE_EN)
            ->update(['content' => $content, 'updated_at' => date('Y-m-d H:i:s')]);
    }
}
