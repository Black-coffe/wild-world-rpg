<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * Правка текста совета `ArmorScreen` (см. 2026-10-24-100000_SeedArmorScreenTip.php):
 * кнопка на карточках брони и оружия подписана «Надеть» (story bugs-info-0923-05,
 * закреплено bugs-info-0923-06). Перезаписывает `content` полным исправленным текстом
 * совета через привязку параметра — идемпотентно, повторный прогон ничего не меняет.
 */
class FixArmorScreenTipNadet extends Migration
{
    public function up(): void
    {
        $content = '👕 *Броня — это не только цифра защиты.* '
            . 'Открыть: *🧑 Я* → *⚔️ Экип* → *👕 Броня / Одежда*. '
            . 'Нажми на любую вещь в списке — откроется её карточка: чего она стоит в бою, '
            . 'как влияет на скорость и скрытность, и кнопка *Надеть* или *Снять*. '
            . 'Одежда и броня хранятся в *Арсенале*, поэтому пока ты его не построил, '
            . 'экран покажет замок и подскажет дорогу к стройке. '
            . 'Голым на остров лучше не выходить: даже рваная рубаха держит удар лучше, чем ничего.';

        $this->db->table('game_tips')
            ->where('title_en', 'ArmorScreen')
            ->update(['content' => $content]);
    }

    public function down(): void
    {
        // Текстовая правка данных, отката нет.
    }
}
