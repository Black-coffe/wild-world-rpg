<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * TIPS-COVERAGE — совет о применении «Мяса диких животных».
 *
 * Повод: игрок в чате сообщества (15.08.2026) заметил, что 🥫 «Тушёнка»
 * готовилась из грибов, зерна и воды — «автор веган?». Он был прав: имя
 * предмета, описание в БД («Мясо-овощная тушёнка в закатанной банке») и арт
 * (банки мясного рагу) обещали мясо, а рецепт его не требовал. Рецепт приведён
 * к остальным трём источникам правды — теперь Тушёнка требует мясо.
 *
 * Совет закрывает вторую половину: «Мясо диких животных» до этого имело ровно
 * одно применение во всей игре (Регенератор) и читалось как мусорный ресурс —
 * игроки не знали, зачем его вообще нести. Теперь у мяса есть кулинарная ниша,
 * и об этом надо сказать.
 *
 * Инварианты (ADR-134): про навигацию и понятия, без хрупких чисел баланса
 * (количества в рецепте живут в Config\CraftRecipes и могут меняться);
 * markdown-safe (парные *); категория «еда» из 14 ENUM; идемпотентность по
 * `title_en`; media-off (ADR-020) — весь смысл в тексте.
 */
class SeedWildMeatUsesTip extends Migration
{
    private const TITLE_EN = 'WildMeatUses';

    public function up(): void
    {
        $now = date('Y-m-d H:i:s');

        if (! empty($this->db->table('game_tips')->where('title_en', self::TITLE_EN)->get()->getRowArray())) {
            return;
        }

        $content = '🥩 *Мясо диких животных нужно не только медикам.* Раньше оно уходило '
            . 'единственной дорогой — в «Регенератор», и многие тащили его мимо. Теперь на нём '
            . 'стоит 🥫 *Тушёнка*: без мяса банку не закатать.'
            . "\n\n"
            . 'Путь к готовке: *«🔨 Крафт»* → *«🔥 Костёр»*. Тушёнка — консерва: она не портится '
            . 'и держит сытость дольше горячего блюда, поэтому её берут в дальние выходы, '
            . 'а не едят у костра.'
            . "\n\n"
            . 'Мясо попадается при добыче в *Горах*, *Тундре*, у *Рек*, в *Тропических джунглях*, '
            . 'на *Полях и равнинах* и в *Пещерах*. В *Лесах* его нет — там ищи грибы и ягоды.';

        $this->db->table('game_tips')->insert([
            'title_ru'   => '🥩 Зачем нужно мясо диких животных',
            'title_en'   => self::TITLE_EN,
            'tip_type'   => 'еда',
            'content'    => $content,
            'created_at' => $now,
            'updated_at' => $now,
        ]);
    }

    public function down(): void
    {
        $this->db->table('game_tips')->where('title_en', self::TITLE_EN)->delete();
    }
}
