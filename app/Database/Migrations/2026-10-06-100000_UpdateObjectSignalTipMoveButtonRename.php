<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * E4 Слайс 2 (срез 07-07): кнопка «🚜 Переехать» переименована в «🧭 Двигаться»
 * (терминологический разрыв: обучающие тексты звали «Двигаться», а в UI кнопки с таким
 * названием не существовало — новички не находили простой ход, обрыв OnbStepMove 33%).
 *
 * Совет ObjectSignal (ADR-098) ссылался на старое имя кнопки → пере-подаём content
 * к актуальному пути. Idempotent: UPDATE к фиксированному значению по title_en
 * (исходный seed Adr098SeedObjectSignalTip идемпотентен по существованию строки и
 * content не обновляет). media-off + markdown-safe (парные *). game_tips = KEEP.
 */
class UpdateObjectSignalTipMoveButtonRename extends Migration
{
    public function up(): void
    {
        $content = '📡 *Сигналы в Походе:* отправляясь в дальний поход (🧭 Двигаться → 🗺 Поход), '
            . 'ты можешь поймать слабый радио-сигнал к редкому объекту поблизости — бункеру, технопарку, '
            . 'городу-призраку. В сигнале — направление и примерная дистанция; иди на него, и сигнал '
            . 'будет крепнуть. Но спеши: источник умолкает, как только кто-то доберётся туда первым. '
            . 'Внутри таких объектов — щедрый лут и прокачка характеристик.';

        $this->db->table('game_tips')
            ->where('title_en', 'ObjectSignal')
            ->update([
                'content'    => $content,
                'updated_at' => date('Y-m-d H:i:s'),
            ]);
    }

    public function down(): void
    {
        // Возврат к тексту исходного seed'а (Adr098SeedObjectSignalTip).
        $content = '📡 *Сигналы в Походе:* отправляясь в дальний поход (🚜 Переехать → Дальний поход), '
            . 'ты можешь поймать слабый радио-сигнал к редкому объекту поблизости — бункеру, технопарку, '
            . 'городу-призраку. В сигнале — направление и примерная дистанция; иди на него, и сигнал '
            . 'будет крепнуть. Но спеши: источник умолкает, как только кто-то доберётся туда первым. '
            . 'Внутри таких объектов — щедрый лут и прокачка характеристик.';

        $this->db->table('game_tips')
            ->where('title_en', 'ObjectSignal')
            ->update([
                'content'    => $content,
                'updated_at' => date('Y-m-d H:i:s'),
            ]);
    }
}
