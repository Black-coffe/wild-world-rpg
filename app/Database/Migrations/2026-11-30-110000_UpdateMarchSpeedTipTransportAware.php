<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * Совет «Скорость Похода» → транспорт-aware формулировка (docs/specs/transport-system,
 * story transport-13). Разворот из `docs/specs/transport-system/concept-final.md` §3:
 * «Живой совет MarchSpeed станет ложью в день релиза» — он утверждал, что темп *постоянный*.
 * После story transport-06 темп задаёт активный транспорт (пешком/повозка/велосипед/снегоход/
 * дрон), а не единое число для всех. Убираем ложное обещание «темп постоянный», сохраняем
 * ключевой смысл: ❤️/💤 — это *топливо* в пути (тратятся за клетку), а не «двигатель», и темп
 * от их уровня не зависит.
 *
 * UPDATE по title_en='MarchSpeed' (идемпотентно, вторая строка не создаётся). Навигация/понятие,
 * без чисел баланса — конкретные cells_per_tick тюнятся в `GameSettings world.vehicle.*`.
 */
class UpdateMarchSpeedTipTransportAware extends Migration
{
    private const NEW_CONTENT = '🚜 *От чего зависит темп Похода?* Отряд идёт ровным шагом, но '
        . 'темп задаёт *транспорт*, а не общее для всех число: пешком один шаг, а повозка, '
        . 'велосипед, снегоход или дрон — свой. ❤️ и 💤 — это *топливо* в пути (тратятся за '
        . 'каждую клетку), а не «двигатель»: от высокого здоровья или выносливости быстрее '
        . 'не станет. Нужен более быстрый темп — крафти транспорт (категория «🚚 Транспорт», '
        . 'с 6 уровня). Нужно далеко — жми «🗺️ Поход» и задавай больше клеток: отряд дойдёт '
        . 'сам, пока ты занят другими делами.';

    private const OLD_CONTENT = '🚜 *Почему Поход идёт с одной скоростью?* Отряд движется '
        . 'ровным шагом и довольно быстро, и темп *постоянный* — он не ускоряется от высокого '
        . 'здоровья или выносливости. ❤️ и 💤 — это *топливо* в пути (тратятся за каждую '
        . 'клетку), а не «двигатель». Нужно далеко — жми «🗺️ Поход» и задавай больше клеток: '
        . 'отряд дойдёт сам, пока ты занят другими делами.';

    public function up(): void
    {
        $this->db->table('game_tips')
            ->where('title_en', 'MarchSpeed')
            ->update(['content' => self::NEW_CONTENT, 'updated_at' => date('Y-m-d H:i:s')]);
    }

    public function down(): void
    {
        $this->db->table('game_tips')
            ->where('title_en', 'MarchSpeed')
            ->update(['content' => self::OLD_CONTENT, 'updated_at' => date('Y-m-d H:i:s')]);
    }
}
