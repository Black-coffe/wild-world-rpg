<?php

declare(strict_types=1);

namespace App\Database\Migrations;

use CodeIgniter\Database\Migration;

/**
 * TIPS-COVERAGE — совет о телепортах: чем маяк отличается от рюкзака и что появился
 * портативный телепорт.
 *
 * Повод: постоянный игрок спросил владельца в чате «как пользоваться телепортом, который
 * не ранец?». Проверка прода показала, что вопрос закономерен: маяки есть у 6 игроков,
 * но установлено их всего 4 и совершено 4 прыжка — механику берут в руки и не понимают,
 * что с ней делать. Ни один из существующих советов телепорт-устройства не разводил.
 * Тем же заходом стал достижимым «Портативный телепорт» (рецепта не существовало вовсе).
 *
 * Инварианты (ADR-134): про навигацию и понятия, без хрупких чисел баланса (заряды,
 * цены и лимиты живут в GameSettings и меняются); markdown-safe (парные *); utf8mb4-эмодзи;
 * категория «крафт» из 14 ENUM; идемпотентность по `title_en`; media-off (ADR-020) —
 * весь смысл в тексте. Надписи кнопок сверены с TeleportBeacon / TeleportBeaconCraft2Select.
 */
class SeedTeleportDevicesTip extends Migration
{
    private const TITLE_EN = 'TeleportDevicesBeaconVsBackpack';

    public function up(): void
    {
        $now = date('Y-m-d H:i:s');

        if (! empty($this->db->table('game_tips')->where('title_en', self::TITLE_EN)->get()->getRowArray())) {
            return;
        }

        $content = '🌀 *Маяк и рюкзак — это не одно и то же.* Их часто путают, а разница простая: '
            . 'рюкзак-телепорт возвращает тебя *домой*, а маяк — это точка, куда ты возвращаешься.'
            . "\n\n"
            . 'Маяк доносят пешком до нужного места и ставят там: *«🏠 База»* → *«📡 Маяки»* → '
            . '*«Установить маяк здесь»*. После этого с любого края карты работает кнопка '
            . '*«Переместиться на маяк»* — в той же двери. Прыжок бесплатен, если с прошлого '
            . 'телепорта прошёл час, иначе возьмут золотом.'
            . "\n\n"
            . 'А ещё в разделе *«🔨 Крафт»* → второй верстак → *«🌀 Телепорты»* теперь собирают '
            . '*портативный телепорт*: он тоже уносит на базу, но ждать между применениями не нужно. '
            . 'Пригодится, когда возвращаться надо срочно и не один раз.';

        $this->db->table('game_tips')->insert([
            'title_ru'   => '🌀 Маяк, рюкзак и портативный: какой телепорт для чего',
            'title_en'   => self::TITLE_EN,
            'tip_type'   => 'крафт',
            'content'    => $content,
            'created_at' => $now,
            'updated_at' => $now,
        ]);
    }

    public function down(): void
    {
        $this->db->table('game_tips')->where('title_en', self::TITLE_EN)->delete();
    }
}
